#!/usr/bin/env node
/* RETIRED fail-closed tombstone: the single-service bubblewrap lane is not a deployable target. */
throw new Error("retired GX10 single-service entrypoint; use the split fd-launcher controller/executor topology");
