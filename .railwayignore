# Keep non-runtime files out of the deployed image (they are not read at runtime).
# Verified: server.mjs and its modules never read docs/, tests, or snapshots.
docs/
.snapshots/
.devdata/
*_test.mjs
*.output

# The local git history (700M+ of loose objects) is never part of the runtime image and
# was timing out the upload. Exclude it and node_modules from what railway up ships.
.git/
node_modules/
