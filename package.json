{
  "name": "dominion-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Dominion AI server — PWA + server-side agent loop (tools, memory, artifacts, persona, flywheel) with a local/cloud model router.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node server.mjs"
  }
}
