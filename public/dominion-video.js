/* Dominion Video Studio — self contained, dependency-free workspace. */
(() => {
  'use strict';
  const API = '/api/video';
  const LEGACY_STORAGE = 'dominion.video.ui.v1';
  const EMPTY_SCREENPLAY_SHA256 = 'e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855';
  const $ = (s, r = document) => r.querySelector(s);
  const $$ = (s, r = document) => [...r.querySelectorAll(s)];
  const uid = () => `local-${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 7)}`;
  const esc = (s = '') => String(s).replace(/[&<>"']/g, c => ({ '&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;' }[c]));
  const clone = value => JSON.parse(JSON.stringify(value));
  const fingerprint = value => { const text=JSON.stringify(value);let hash=2166136261;for(let i=0;i<text.length;i++){hash^=text.charCodeAt(i);hash=Math.imul(hash,16777619);}return `${text.length}-${(hash>>>0).toString(16)}`; };
  const device = () => {
    const nav = typeof navigator === 'undefined' ? {} : navigator;
    const ua = String(nav.userAgent || '');
    const declaredMobile = nav.userAgentData?.mobile === true || /Android|iPhone|iPad|iPod|IEMobile|Opera Mini|Mobile/i.test(ua);
    const touchTablet = Number(nav.maxTouchPoints || 0) > 1 && (/Macintosh/i.test(ua) || /Tablet|Silk|Kindle/i.test(ua));
    return declaredMobile || touchTablet ? 'mobile' : 'desktop';
  };
  const tokenBytes = new TextEncoder();
  const tokenEstimate = text => { const value=String(text||'');return Math.max(value.trim()?value.trim().split(/\s+/).length:0,Math.ceil(tokenBytes.encode(value).byteLength/3)); };
  const currentPrivacyMode = () => { const visible=document.getElementById('privacy-mode')?.value; if(visible)return visible; try{return localStorage.getItem('dominion.privacy-mode.v1')||'normal';}catch{return 'normal';} };
  const models = [
    ['google:gemini@omni-flash', 'Gemini Omni Flash'], ['bytedance:seedance@2.0', 'Seedance 2.0'],
    ['klingai:kling-video@3.0-turbo', 'Kling 3.0 Turbo'], ['xai:grok-imagine@video-1.5', 'Grok Imagine 1.5']
  ];
  const defaults = () => ({
    open: false, project: { id: null, name: 'Untitled video', model: models[0][0], purpose: 'Social campaign', platform: 'YouTube', ratio: '16:9', resolution: '720p', format: 'mp4', duration: 30, folder: null }, projectRevision:null, screenplaySha256: null,
    screenplay: '', scenes: [], tracks: [
      { id:'v1', type:'video', name:'Video 1', mute:false, solo:false, lock:false }, { id:'v2', type:'video', name:'Video 2', mute:false, solo:false, lock:false }, { id:'v3', type:'video', name:'Video 3', mute:false, solo:false, lock:false },
      { id:'a1', type:'audio', name:'Audio 1', mute:false, solo:false, lock:false }, { id:'a2', type:'audio', name:'Audio 2', mute:false, solo:false, lock:false }, { id:'a3', type:'audio', name:'Audio 3', mute:false, solo:false, lock:false }, { id:'a4', type:'audio', name:'Audio 4', mute:false, solo:false, lock:false }
    ], clips: [], selected: null, playhead: 0, zoom: 1, playing:false, focus:false, panels:{ writer:'regular', board:'regular' }, tray:[], history:[], redo:[], checkpoint: 'Saved', job: null, generationIntent:null, desktopSession: true, config: null, messages: [],
    inflight:{generation:false,chat:false,screenwriter:false,import:false,export:false}, screenwriterRecovery:{pending:false,projectId:null,status:'idle',actionRequired:false,recoveryAction:null,operatorReference:null,quarantineConfirmation:null,message:''}, drafts:{chat:'',mobilePrompt:'',mobileMedia:{mode:'text',duration:5,frameImages:'',referenceImages:'',referenceVideos:'',referenceAudios:'',sourceVideo:'',videoId:'',shots:'',generateAudio:true}}
  });
  let state = defaults(), root, lastFocus, studioReturnFocus, drag, folderHandle, presenceTimer, creatingProject, screenwriterSetup, scriptSaveTimer, preferenceKey = null, activeDialog = null, backgroundState = [], conflictDraft = null;
  let saveQueue = Promise.resolve(), checkpointEpoch = 0, lastSaveFailure = null, projectSwitching = null, closing = false, editRevision = 0, savedRevision = 0;
  const activeJobs = new Map();
  function save() {
    if (!preferenceKey) return;
    try { localStorage.setItem(preferenceKey, JSON.stringify({ projectId:state.project.id||null, panels:state.panels, focus:!!state.focus, zoom:Number(state.zoom)||1, generationIntent:state.generationIntent, screenwriterRecovery:state.screenwriterRecovery?.pending?state.screenwriterRecovery:null })); } catch {}
  }
  function loadPreferences(key) {
    preferenceKey = typeof key === 'string' && key.trim() ? key.trim() : null;
    try { localStorage.removeItem(LEGACY_STORAGE); } catch {}
    if (!preferenceKey) return;
    try {
      const prefs=JSON.parse(localStorage.getItem(preferenceKey)||'{}');
      if(typeof prefs.projectId==='string'&&prefs.projectId.length<100)state.project.id=prefs.projectId;
      if(prefs.panels&&['writer','board'].every(k=>['regular','expanded','minimized'].includes(prefs.panels[k])))state.panels=clone(prefs.panels);
      if(Number(prefs.zoom)>=.5&&Number(prefs.zoom)<=3)state.zoom=Number(prefs.zoom);
      if(prefs.generationIntent&&/^[A-Za-z0-9_-]{1,128}$/.test(String(prefs.generationIntent.key||''))&&/^\d+-[a-f0-9]+$/.test(String(prefs.generationIntent.fingerprint||'')))state.generationIntent={key:String(prefs.generationIntent.key),fingerprint:String(prefs.generationIntent.fingerprint)};
      if(prefs.screenwriterRecovery?.pending===true&&prefs.screenwriterRecovery.projectId===state.project.id)state.screenwriterRecovery={pending:true,projectId:state.project.id,status:String(prefs.screenwriterRecovery.status||'checking').slice(0,80),actionRequired:!!prefs.screenwriterRecovery.actionRequired,recoveryAction:String(prefs.screenwriterRecovery.recoveryAction||'').slice(0,80)||null,operatorReference:String(prefs.screenwriterRecovery.operatorReference||'').slice(0,300)||null,quarantineConfirmation:String(prefs.screenwriterRecovery.quarantineConfirmation||'').slice(0,400)||null,message:String(prefs.screenwriterRecovery.message||'Checking the durable Trinity turn.').slice(0,800)};
      state.focus=!!prefs.focus;
    } catch {}
  }
  function readable(error) { const message=error?.message || 'The video service could not complete that request. Nothing was marked as finished.'; return error?.code ? `${error.code}: ${message}` : message; }
  function prepareGenerationBody(raw) {
    const body=JSON.parse(String(raw||'{}')),mode=String(body.mode||'text');
    if(mode!=='image')delete body.frameImages;
    if(!['reference','edit','continue'].includes(mode)){delete body.referenceImages;delete body.referenceVideos;delete body.referenceAudios;}
    if(mode!=='edit')delete body.sourceVideo;if(mode!=='continue')delete body.videoId;
    const requestedKey=String(body.idempotencyKey||`gen-${globalThis.crypto?.randomUUID?.()||uid()}`),identity=clone(body);delete identity.idempotencyKey;
    const nextFingerprint=fingerprint(identity),key=state.generationIntent?.fingerprint===nextFingerprint?state.generationIntent.key:requestedKey;
    state.generationIntent={key,fingerprint:nextFingerprint};save();body.idempotencyKey=key;return JSON.stringify(body);
  }
  async function eventStreamResult(res) {
    if(!res.body?.getReader)throw Object.assign(new Error('The browser could not keep the Trinity request open.'),{code:'screenwriter_stream_unavailable'});
    const reader=res.body.getReader(),decoder=new TextDecoder();let buffer='',result=null;
    const consume=block=>{let event='message',data=[];for(const line of block.split('\n')){if(line.startsWith(':'))continue;if(line.startsWith('event:'))event=line.slice(6).trim();else if(line.startsWith('data:'))data.push(line.slice(5).trimStart());}if(!data.length)return;let payload;try{payload=JSON.parse(data.join('\n'));}catch{throw Object.assign(new Error('Dominion returned a malformed screenplay progress event.'),{code:'screenwriter_stream_invalid'});}if(event==='error'){const detail=typeof payload?.error==='string'?payload.error:payload?.error?.message||payload?.message;throw Object.assign(new Error(detail||'Trinity could not complete the saved screenplay section.'),{code:payload?.error?.code||payload?.code,status:payload?.status});}if(event==='result')result=payload;};
    while(true){const part=await reader.read();buffer=(buffer+decoder.decode(part.value||new Uint8Array(),{stream:!part.done})).replace(/\r\n/g,'\n');let boundary;while((boundary=buffer.indexOf('\n\n'))>=0){const block=buffer.slice(0,boundary);buffer=buffer.slice(boundary+2);consume(block);}if(part.done)break;}
    if(buffer.trim())consume(buffer);if(!result)throw Object.assign(new Error('Dominion closed the Trinity request without a saved result.'),{code:'screenwriter_stream_incomplete'});return result;
  }
  async function request(path, opts = {}) {
    const eventStream=opts.eventStream===true,requestOpts={...opts};delete requestOpts.eventStream;
    const generationRequest=path==='/jobs'&&String(requestOpts.method||'GET').toUpperCase()==='POST'&&requestOpts.body;
    const init = { credentials:'same-origin', ...requestOpts, headers:{ ...(requestOpts.body ? { 'Content-Type':'application/json' } : {}), ...(eventStream?{Accept:'text/event-stream'}:{}), 'X-Dominion-Device':device(), ...(state.config?.desktopCapability ? { 'X-Dominion-Desktop-Capability':state.config.desktopCapability } : {}), ...(requestOpts.headers || {}) }, ...(generationRequest?{body:prepareGenerationBody(requestOpts.body)}:{}) };
    const res = await fetch(`${API}${path}`, init);
    const streamed=String(res.headers?.get?.('content-type')||'').toLowerCase().includes('text/event-stream');
    let data; try { data=streamed?await eventStreamResult(res):await res.json(); } catch(error){if(streamed)throw error;data=null;}
    if(generationRequest&&((res.ok&&(data?.id||data?.jobId))||(!res.ok&&res.status<500&&![408,429].includes(res.status)))){state.generationIntent=null;save();}
    if (!res.ok) { const detail=typeof data?.error==='string'?data.error:data?.error?.message||data?.message; const error=new Error(detail||`Video service returned ${res.status}.`); error.code=data?.error?.code||data?.code; error.status=res.status; throw error; }
    return data;
  }
  function toast(text, kind = '') { const n = $('#dv-notice', root); if (!n) return; n.textContent = text; n.className = `dv-notice show ${kind}`; clearTimeout(toast.t); toast.t = setTimeout(() => n.className='dv-notice', 5200); }
  async function ensureProject() {
    if (state.project.id) return state.project.id;
    if (creatingProject) return creatingProject;
    const epoch=checkpointEpoch,target=state;
    const pending=(async()=>{ const made=await request('/projects',{method:'POST',body:JSON.stringify({name:target.project.name})}); const payload=made.project?.project?made.project:made; const created=payload.project||made,id=created.id||made.id||null;if(!id)throw new Error('The project service did not return a project id.');if(epoch===checkpointEpoch&&state===target){state.project.id=id;state.project.createdAt=created.createdAt||state.project.createdAt;state.project.updatedAt=created.updatedAt||state.project.updatedAt;state.projectRevision=Number.isInteger(Number(payload.projectRevision))?Number(payload.projectRevision):null;state.screenplaySha256=/^[a-f0-9]{64}$/.test(String(payload.screenplaySha256||''))?payload.screenplaySha256:EMPTY_SCREENPLAY_SHA256;save();}return id; })();creatingProject=pending;
    try{return await pending;}finally{if(creatingProject===pending)creatingProject=null;}
  }
  const isMobileSingle = () => state.config?.singleGenerationOnly === true;
  const fullEditor = () => state.config ? state.config.fullEditor !== false : device() !== 'mobile';
  const writerStateLocked = () => state.inflight.screenwriter||state.screenwriterRecovery?.pending;
  const projectEditLocked = () => !!conflictDraft||writerStateLocked()||state.inflight.chat||generationBusy()||state.inflight.import||state.inflight.export;
  async function mirrorProject(payload = projectPayload()) {
    if(!folderHandle)return;
    const safe=String(state.project.name||'video-project').replace(/[<>:"/\\|?*]/g,'-').slice(0,120)||'video-project';
    const projectId=String(payload.project?.id||state.project.id||'unsaved').replace(/[^A-Za-z0-9_-]/g,'-');
    const file=await folderHandle.getFileHandle(`${safe}-${projectId}.dominion-video.json`,{create:true}); const writer=await file.createWritable(); await writer.write(JSON.stringify({schemaVersion:1,savedAt:new Date().toISOString(),...payload},null,2)); await writer.close();
  }
  async function createCheckpointProject(snapshot, epoch) {
    if (snapshot.project?.id) return snapshot.project.id;
    if (epoch !== checkpointEpoch) throw new Error('Checkpoint was cancelled because the active project changed.');
    if (state.project.id) { snapshot.project.id=state.project.id; return state.project.id; }
    const id=await ensureProject();
    if(epoch!==checkpointEpoch||!state.open)throw new Error('Checkpoint was cancelled because the active project changed.');
    snapshot.project.id=id;
    return id;
  }
  async function flushCheckpoints() {
    try { await saveQueue; lastSaveFailure=null; }
    catch (error) { lastSaveFailure=error; throw error; }
  }
  async function persistDirtyEdits(label){const hadScriptSave=!!scriptSaveTimer;clearTimeout(scriptSaveTimer);scriptSaveTimer=null;if(hadScriptSave||editRevision>savedRevision)await checkpoint(label);await flushCheckpoints();if(lastSaveFailure)throw lastSaveFailure;}
  const sameSession=(projectId,epoch)=>state.open&&checkpointEpoch===epoch&&state.project.id===projectId;
  function setScreenwriterRecovery(projectId,detail={}){if(state.project.id!==projectId)return;const prior=state.screenwriterRecovery?.projectId===projectId?state.screenwriterRecovery:null,recoveryAction=Object.hasOwn(detail,'recoveryAction')?String(detail.recoveryAction||'').slice(0,80)||null:prior?.recoveryAction||null,operatorReference=Object.hasOwn(detail,'operatorReference')?String(detail.operatorReference||'').slice(0,300)||null:prior?.operatorReference||null,quarantineConfirmation=Object.hasOwn(detail,'quarantineConfirmation')?String(detail.quarantineConfirmation||'').slice(0,400)||null:prior?.quarantineConfirmation||null;state.screenwriterRecovery={pending:true,projectId,status:String(detail.status||'checking').slice(0,80),actionRequired:detail.actionRequired===true,recoveryAction,operatorReference,quarantineConfirmation,message:String(detail.message||'Checking the durable Trinity turn.').slice(0,800)};save();}
  function clearScreenwriterRecovery(projectId){if(state.project.id!==projectId)return;state.screenwriterRecovery={pending:false,projectId:null,status:'idle',actionRequired:false,recoveryAction:null,operatorReference:null,quarantineConfirmation:null,message:''};save();}
  async function reconcileScreenwriterProject(projectId,{epoch=checkpointEpoch,waitForTerminal=false}={}){
    if(!projectId)return false;const deadline=Date.now()+315000;
    while(sameSession(projectId,epoch)){
      try{
        const status=await request(`/screenwriter/status?projectId=${encodeURIComponent(projectId)}`);if(!sameSession(projectId,epoch))return false;
        if(!status.pending){const loaded=await request(`/projects/${encodeURIComponent(projectId)}`);if(!sameSession(projectId,epoch))return false;applyPayload(loaded.project?.project?loaded.project:loaded);clearScreenwriterRecovery(projectId);render();return true;}
        setScreenwriterRecovery(projectId,status);render();
        if(status.actionRequired){const loaded=await request(`/projects/${encodeURIComponent(projectId)}`);if(!sameSession(projectId,epoch))return false;applyPayload(loaded.project?.project?loaded.project:loaded);setScreenwriterRecovery(projectId,status);render();}
        if(status.actionRequired)return false;
        if(!waitForTerminal||Date.now()>=deadline){setScreenwriterRecovery(projectId,{status:'status_retry_required',actionRequired:true,recoveryAction:'retry_status',message:'The Trinity turn is still saved, but its terminal status was not available yet. Check its saved status again before editing.'});render();return false;}
        await new Promise(resolve=>setTimeout(resolve,Math.min(5000,Math.max(1000,Number(status.retryAfterMs)||3000))));
      }catch(error){
        if(!sameSession(projectId,epoch))return false;
        setScreenwriterRecovery(projectId,{status:'status_unavailable',message:`Dominion could not yet verify the saved Trinity turn. ${readable(error)}`});render();
        if(!waitForTerminal||Date.now()>=deadline){setScreenwriterRecovery(projectId,{status:'status_unavailable',actionRequired:true,recoveryAction:'retry_status',message:`Dominion could not verify the saved Trinity turn. Check its saved status again before editing. ${readable(error)}`});render();return false;}
        await new Promise(resolve=>setTimeout(resolve,3000));
      }
    }
    return false;
  }
  async function reconcileScreenwriterNow(){
    const projectId=state.project.id,epoch=checkpointEpoch,action=state.screenwriterRecovery?.recoveryAction;
    if(!projectId||!state.screenwriterRecovery?.actionRequired)return;
    if(action==='quarantine_unrecoverable')return screenwriterQuarantineDialog();
    if(!['retry_settlement','check_generation','retry_status'].includes(action))return toast('This saved Trinity request needs operator reconciliation because no safe provider identity is available.','error');
    if(state.inflight.screenwriter)return;state.inflight.screenwriter=true;setScreenwriterRecovery(projectId,{...state.screenwriterRecovery,status:'reconciling',actionRequired:false,message:'Dominion is reconciling the saved Trinity turn without starting another screenplay request.'});render();
    try{if(action==='retry_status'){setScreenwriterRecovery(projectId,{status:'checking',actionRequired:false,recoveryAction:'retry_status',message:'Checking the authoritative saved Trinity status.'});const refreshed=await reconcileScreenwriterProject(projectId,{epoch,waitForTerminal:false});if(sameSession(projectId,epoch)&&refreshed)toast('The saved Trinity turn is verified.');return;}const d=await request('/screenwriter/reconcile',{method:'POST',body:JSON.stringify({projectId,privacyMode:currentPrivacyMode()})});if(!sameSession(projectId,epoch))return;if(d.project)applyPayload(d.project);setScreenwriterRecovery(projectId,{status:'checking',message:'Checking the complete Trinity ledger after repair.'});await reconcileScreenwriterProject(projectId,{epoch,waitForTerminal:false});if(sameSession(projectId,epoch))toast(d.message||'The saved Trinity turn was reconciled.');}
    catch(error){if(sameSession(projectId,epoch)){setScreenwriterRecovery(projectId,{status:'checking',message:'Refreshing the authoritative Trinity repair status.'});await reconcileScreenwriterProject(projectId,{epoch,waitForTerminal:false});if(sameSession(projectId,epoch))toast(readable(error),'error');}}
    finally{if(sameSession(projectId,epoch)){state.inflight.screenwriter=false;render();}}
  }
  async function prepareProjectSwitch() {
    if(state.inflight.screenwriter||(state.screenwriterRecovery?.pending&&!state.screenwriterRecovery.actionRequired)||state.inflight.chat||state.inflight.generation||state.inflight.import||state.inflight.export){toast('Wait for the active project operation to reach its durable checkpoint before switching projects.','error');return false;}
    try { await persistDirtyEdits('Saved edits before project switch'); }
    catch (error) { toast(`Project was not switched. ${readable(error)}`,'error'); return false; }
    checkpointEpoch++;
    editRevision=0;savedRevision=0;
    for(const [id,entry] of activeJobs){if(entry.projectId===state.project.id){clearTimeout(entry.timer);activeJobs.delete(id);}}
    return true;
  }
  function checkpoint(label, before) {
    if (isMobileSingle()) return Promise.resolve();
    const checkpointLabel=label||'Saved checkpoint',snapshot=clone(projectPayload()),epoch=checkpointEpoch,projectAtQueue=snapshot.project.id||null,revisionAtQueue=editRevision;
    state.checkpoint='Saving…';save();paintStatus();
    saveQueue=saveQueue.catch(()=>{}).then(async()=>{
      if(epoch!==checkpointEpoch)return{cancelled:true};
      const projectId=projectAtQueue||await createCheckpointProject(snapshot,epoch);
      if(epoch!==checkpointEpoch||(projectAtQueue&&state.project.id!==projectAtQueue))return{cancelled:true};
      const expectedScreenplaySha256=String(state.screenplaySha256||''),expectedProjectRevision=Number(state.projectRevision);
      if(!/^[a-f0-9]{64}$/.test(expectedScreenplaySha256))throw Object.assign(new Error('Refresh this project before saving; its screenplay revision could not be verified.'),{code:'video_screenplay_precondition_required'});
      if(!Number.isInteger(expectedProjectRevision)||expectedProjectRevision<0)throw Object.assign(new Error('Refresh this project before saving; its project revision could not be verified.'),{code:'video_project_revision_required'});
      snapshot.project.id=projectId;
      const saved=await request('/projects/checkpoint',{method:'POST',body:JSON.stringify({projectId,label:checkpointLabel,state:snapshot,expectedScreenplaySha256,expectedProjectRevision})});
      if(!/^[a-f0-9]{64}$/.test(String(saved.screenplaySha256||''))||!Number.isInteger(Number(saved.projectRevision)))throw Object.assign(new Error('Dominion saved the checkpoint but did not return its complete project revision. Refresh before making another change.'),{code:'video_checkpoint_revision_missing'});
      try{await mirrorProject(snapshot);}catch(error){toast(`Server checkpoint saved; folder mirror needs attention. ${readable(error)}`,'error');}
      if(epoch===checkpointEpoch&&state.project.id===projectId){state.screenplaySha256=saved.screenplaySha256;state.projectRevision=Number(saved.projectRevision);savedRevision=Math.max(savedRevision,revisionAtQueue);state.checkpoint=checkpointLabel;lastSaveFailure=null;save();paintStatus();}
      return saved;
    }).catch(error=>{
      if(epoch===checkpointEpoch){
        lastSaveFailure=error;
        const conflict=['video_project_revision_stale','video_checkpoint_stale'].includes(String(error?.code||''));
        if(conflict){const projectId=projectAtQueue||state.project.id,current=state.project.id===projectId?projectPayload():snapshot;conflictDraft={projectId,label:checkpointLabel,capturedAt:new Date().toISOString(),snapshot:clone(current)};state.checkpoint='Save conflict · local draft held';save();render(false);toast('A newer server checkpoint exists. Your local draft is held; preserve it and reload from the conflict button.','error');}
        else{state.checkpoint='Save needs attention';save();paintStatus();toast(`Server checkpoint failed. ${readable(error)}`,'error');}
      }
      throw error;
    });
    return saveQueue;
  }
  function projectPayload() { return { project:state.project, screenplay:state.screenplay, scenes:state.scenes, tracks:state.tracks, clips:state.clips, messages:state.messages, ui:{panels:state.panels, focus:state.focus, zoom:state.zoom} }; }
  function mutate(label, fn) { const editLocked=projectEditLocked(),uiOnly=/^(Minimized|Expanded|Restored|Entered editor focus|Exited editor focus|Changed timeline zoom)/.test(String(label||''));if(editLocked&&uiOnly){fn();save();render();return;}if(projectSwitching||editLocked){toast(projectSwitching?'Finish the current project action before editing.':'That project change is paused until the active durable operation finishes or reconciles.','error');render(false);return;}const before = clone(projectPayload()); fn();editRevision++; checkpoint(label, before); render(); }
  async function historyCommand(command) {
    if(isMobileSingle())return;
    if(projectSwitching||projectEditLocked())return toast('Saved history is paused until the current project operation finishes or reconciles.','error');
    if(!state.project.id)return toast('Make and save the first project change before using history.','error');
    const projectId=state.project.id,epoch=checkpointEpoch;
    try{await runProjectSwitch(null,async()=>{await persistDirtyEdits(`Saved edits before ${command}`);if(!sameSession(projectId,epoch))return;const expectedProjectRevision=Number(state.projectRevision);if(!Number.isInteger(expectedProjectRevision))throw Object.assign(new Error('Refresh this project before changing saved history.'),{code:'video_project_revision_required'});const payload=await request(`/projects/${encodeURIComponent(projectId)}/${command}`,{method:'POST',body:JSON.stringify({expectedProjectRevision})});if(!sameSession(projectId,epoch))return;applyPayload(payload.project?.project?payload.project:payload);state.history=[];state.redo=[];save();render();toast(command==='undo'?'Project change undone.':'Project change restored.');});}
    catch(error){if(sameSession(projectId,epoch))toast(readable(error),'error');}
  }
  const undo=()=>historyCommand('undo');
  const redo=()=>historyCommand('redo');
  function applyPayload(p) { Object.assign(state.project,p.project);state.projectRevision=Number.isInteger(Number(p.projectRevision))?Number(p.projectRevision):null; state.screenplay=p.screenplay||'';state.screenplaySha256=/^[a-f0-9]{64}$/.test(String(p.screenplaySha256||''))?p.screenplaySha256:null; state.scenes=p.scenes||[]; state.tracks=p.tracks||state.tracks; state.clips=p.clips||[]; state.messages=p.messages||state.messages||[];editRevision=0;savedRevision=0; if(!preferenceKey)Object.assign(state,p.ui||{}); }
  function modelLabel(id){return models.find(([key])=>key===id)?.[1]||String(id||'Unknown model').split('/').at(-1);}
  function modelEntries(){const configured=state.config?.capabilities||state.config?.models;return configured&&Object.keys(configured).length?Object.keys(configured).map(id=>[id,modelLabel(id)]):models;}
  function modelOptions(value) { const entries=modelEntries(),known=entries.some(([id])=>id===value);return `${!known&&value?`<option value="${esc(value)}" selected disabled>Unavailable · ${esc(modelLabel(value))}</option>`:''}${entries.map(([id,n]) => `<option value="${esc(id)}" ${id===value?'selected':''}>${esc(n)}</option>`).join('')}`; }
  function capability(model=state.project.model){return state.config?.capabilities?.[model]||state.config?.models?.[model]||null;}
  function options(values,current,labels={}){return (values||[]).map(value=>`<option value="${esc(value)}" ${String(value)===String(current)?'selected':''}>${esc(labels[value]||value)}</option>`).join('');}
  function lines(value){return Array.isArray(value)?value.join('\n'):String(value||'');}
  function mediaLines(value){return String(value||'').split(/\r?\n/).map(item=>item.trim()).filter(Boolean);}
  function agentMarkup(role,label){const item=state.config?.agents?.[role]||{};return `<b>${esc(label)}</b><span>${esc(item.model||'Not configured')}</span><em class="${item.configured?'ready':'unavailable'}">${item.configured?'ready':'unavailable'}</em>`;}
  function generationBusy(){return state.inflight.generation||activeJobs.size>0;}
  function previewMarkup(){const clip=selectedClip()||state.clips.find(c=>c.type==='video'&&c.src);if(clip?.src)return `<video id="dv-preview-video" controls playsinline preload="metadata" src="${esc(clip.src)}"></video>`;return `<div class="dv-player-empty"><span>◇</span><b>${state.clips.length ? 'Select a clip to preview' : 'Your film begins here'}</b><small>${state.clips.length ? 'The selected clip will play in this monitor.' : 'Write a scene or ask the director to create one.'}</small></div>`;}
  function baseMarkup() {
    const single=isMobileSingle(), cap=capability(), mobile=state.drafts.mobileMedia, modes=cap?.modes||['text'];
    const mode=modes.includes(mobile.mode)?mobile.mode:modes[0], screenwriterAvailable=state.config?.agents?.screenwriter?.configured===true&&state.config?.screenwriter?.available===true;
    const writerRecovering=state.screenwriterRecovery?.pending===true,projectOperationBusy=!!projectSwitching||state.inflight.chat||generationBusy()||state.inflight.import||state.inflight.export,generationDisabled=generationBusy()||!!projectSwitching||(!single&&projectEditLocked())?'disabled aria-disabled="true"':'', chatDisabled=projectEditLocked()||!!projectSwitching?'disabled aria-disabled="true"':'', writerDisabled=!screenwriterAvailable||state.inflight.screenwriter||writerRecovering||projectOperationBusy;
    const messageMarkup=state.messages.map(m=>`<article class="${m.role==='user'?'user':m.role==='status'?'degraded':''}">${m.role==='assistant'?'<b>Video liaison</b>':m.role==='status'?'<b>Orchestration status</b>':''}<p>${esc(m.code?`${m.code}: ${m.content}`:m.content)}</p></article>`).join('');
    const mobileMedia=`<details class="dv-mobile-generation"><summary>Model inputs</summary><div class="dv-generation-grid"><label>Mode<select id="dv-mobile-mode">${options(modes,mode)}</select></label><label>Duration<input id="dv-mobile-duration" type="number" min="${cap?.minDuration||1}" max="${cap?.maxDuration||15}" step="1" value="${Number(mobile.duration)||cap?.minDuration||5}"></label>${cap?.maxFrameImages?`<label>Frame image URLs <small>one per line, max ${cap.maxFrameImages}</small><textarea id="dv-mobile-frames">${esc(mobile.frameImages)}</textarea></label>`:''}${cap?.maxReferenceImages?`<label>Reference image URLs <small>one per line, max ${cap.maxReferenceImages}</small><textarea id="dv-mobile-ref-images">${esc(mobile.referenceImages)}</textarea></label>`:''}${cap?.maxReferenceVideos?`<label>Reference video URLs <small>one per line, max ${cap.maxReferenceVideos}</small><textarea id="dv-mobile-ref-videos">${esc(mobile.referenceVideos)}</textarea></label>`:''}${cap?.maxReferenceAudio?`<label>Reference audio URLs <small>one per line, max ${cap.maxReferenceAudio}</small><textarea id="dv-mobile-ref-audios">${esc(mobile.referenceAudios)}</textarea></label>`:''}${modes.includes('edit')?`<label>Source video URL<input id="dv-mobile-source" value="${esc(mobile.sourceVideo)}"></label>`:''}${modes.includes('continue')?`<label>Prior video ID<input id="dv-mobile-video-id" value="${esc(mobile.videoId)}"></label>`:''}${cap?.maxShots?`<label>Kling shots JSON<textarea id="dv-mobile-shots" placeholder='[{"duration":3,"prompt":"..."}]'>${esc(mobile.shots)}</textarea></label>`:''}<label class="dv-check"><input id="dv-mobile-audio" type="checkbox" ${mobile.generateAudio!==false?'checked':''}> Generate audio</label></div></details>`;
    return `<div class="dv-shell">
      <header class="dv-top"><button class="dv-mark" ${single?'disabled':''} data-action="dashboard" aria-label="Open video projects"><span>◈</span> DOMINION <b id="dv-studio-name">VIDEO</b></button><div class="dv-project-name">${single?'<span class="dv-single-title">MOBILE SINGLE GENERATION</span>':`<input id="dv-project-name" value="${esc(state.project.name)}" aria-label="Project name"><span id="dv-checkpoint">${esc(state.checkpoint)}</span>`}</div><div class="dv-top-actions">${single?'':`${conflictDraft?'<button class="dv-primary" data-action="conflict">Resolve save conflict</button>':''}<button data-action="undo" title="Undo saved change (Ctrl+Z)" aria-label="Undo saved change">↶</button><button data-action="redo" title="Redo saved change (Ctrl+Shift+Z)" aria-label="Redo saved change">↷</button><button data-action="history">History</button><button data-action="focus" id="dv-focus" title="Focus player and sequence editor">⛶ Player + Editor</button><button data-action="dashboard">Projects</button><button class="dv-primary" data-action="export">Export</button>`}<button class="dv-close" data-action="close" aria-label="Return to Dominion">×</button></div></header>
      <section class="dv-controlbar" aria-label="Video settings"><label>Default model<select id="dv-model">${modelOptions(state.project.model)}</select></label><label>Purpose<input id="dv-purpose" value="${esc(state.project.purpose)}"></label><label>Platform<select id="dv-platform">${options(['YouTube','TikTok','Instagram Reels','LinkedIn','Generic'],state.project.platform)}</select></label><label>Ratio<select id="dv-ratio">${options(cap?.ratios||['16:9'],state.project.ratio)}</select></label><label>Resolution<select id="dv-resolution">${options(cap?.resolutions||['720p'],state.project.resolution,{ '4k':'4K' })}</select></label><label>Format<select id="dv-format">${options(['mp4','mov','webm'],state.project.format)}</select></label><span class="dv-estimate" id="dv-estimate">Estimated after plan</span><button class="dv-generate" data-action="generate" ${generationDisabled}>${generationBusy()?'Generation running':'Generate next scene'}</button></section>
      ${state.config&&state.config.provider&&state.config.provider.runwareConfigured===false?`<div class="dv-config-banner" role="alert"><b>Video generation is not configured.</b> The render engine's key (Runware) is missing on the server, so nothing can generate yet. Planning, chat, the screenwriter, and the storyboard still work. <small>Server owner: set RUNWARE_VIDEO_GEN_DOMINION_API_KEY and redeploy.</small></div>`:''}
      <main class="dv-workspace">
        <section class="dv-panel dv-writer" id="dv-writer" data-panel="writer"><header><span><i>✦</i> SCREENWRITER <small>${esc(state.config?.agents?.screenwriter?.model||'Not configured')}</small></span><div><button data-action="panel-expand" data-panel="writer" aria-label="Expand screenwriter">↗</button><button data-action="panel-minimize" data-panel="writer" aria-label="Minimize screenwriter">⌄</button></div></header><textarea id="dv-script" maxlength="460000" placeholder="Describe the story, characters, tone, and audience." ${state.inflight.screenwriter||writerRecovering||projectOperationBusy?'disabled aria-disabled="true"':''}></textarea><footer><span id="dv-script-count">0 / 115,000 tokens</span><button data-action="screenwrite" ${writerDisabled?'disabled aria-disabled="true"':''} title="${esc(!screenwriterAvailable?state.config?.screenwriter?.message||'Screenwriter unavailable':state.inflight.screenwriter?'Screenwriter request running':writerRecovering?state.screenwriterRecovery.message:projectOperationBusy?'Another project operation is running':'Write next section')}">${!screenwriterAvailable?'Screenwriter unavailable':state.inflight.screenwriter?'Writing…':writerRecovering?(state.screenwriterRecovery.actionRequired?'Reconciliation needed':'Recovering…'):projectOperationBusy?'Project busy':'Write next section'}</button></footer></section>
        <section class="dv-stage" aria-label="Selected clip preview"><div class="dv-player" id="dv-player"><div class="dv-player-grid"></div>${previewMarkup()}<div class="dv-safe">${esc(state.project.ratio)} · ${esc(state.project.resolution)}</div><button data-action="play" class="dv-play" aria-label="Play selected clip preview">${state.playing?'❚❚':'▶'}</button></div>${single?`<label class="dv-single-prompt">VIDEO PROMPT<textarea id="dv-single-prompt" rows="5" maxlength="32000" placeholder="Describe the video you want to generate…">${esc(state.drafts.mobilePrompt)}</textarea><small>One verified video will download to this phone. Run Dominion on desktop to create a saved project.</small></label>${mobileMedia}`:`<div class="dv-agent-strip"><span class="dv-pulse"></span>${agentMarkup('director','Creative Director')}<i>·</i>${agentMarkup('visualOrchestrator','Visual Orchestrator')}<i>·</i>${agentMarkup('screenwriter','Screenwriter')}<i>·</i>${agentMarkup('liaison','Liaison')}<button data-action="open-chat">Open chat ↗</button></div><small class="dv-crew-line" title="The studio's transports are fixed castings, so the privacy story is stated here, where it applies. Private mode still blocks the studio.">This studio runs its own fixed crew — Runware · NVIDIA · OpenRouter · Anthropic — in Normal and Trusted privacy modes.</small>`}</section>
        <section class="dv-panel dv-board" id="dv-board" data-panel="board"><header><span><i>◫</i> STORYBOARD <small><b id="dv-scene-count">0</b> / 100 scenes</small></span><div><button data-action="panel-expand" data-panel="board" aria-label="Expand storyboard">↗</button><button data-action="panel-minimize" data-panel="board" aria-label="Minimize storyboard">⌄</button></div></header><div class="dv-scenes" id="dv-scenes"></div><footer><button data-action="add-scene">+ Add scene</button><button data-action="generate" ${generationDisabled}>${generationBusy()?'Generation running':'Generate selected'}</button></footer></section>
        <section class="dv-scene-previews" aria-label="Scene previews"><header><span>SCENE PREVIEWS</span><button data-action="focus">Open player + editor ↗</button></header><div class="dv-preview-strip" id="dv-preview-strip"></div></section>
        <aside class="dv-chat" id="dv-chat"><div class="dv-chat-head"><span><i class="dv-pulse"></i> AI CHAT · Liaison ${state.config?.agents?.liaison?.configured?'ready':'unavailable'}</span><button data-action="mobile-nav">Panels</button></div><div class="dv-chat-body" id="dv-chat-body"><article><b>Video liaison</b><p>I’ll keep the screenplay, director, and timeline aligned. Tell me the outcome you want, and I’ll save the resulting plan.</p></article>${messageMarkup}</div><form id="dv-chat-form"><textarea id="dv-chat-input" rows="2" placeholder="Ask about your video…" aria-label="Message video liaison" ${chatDisabled}>${esc(state.drafts.chat)}</textarea><button aria-label="Send message" ${chatDisabled}>${state.inflight.chat?'…':'↑'}</button></form></aside>
        <div class="dv-tray" id="dv-tray" aria-label="Minimized creative panels"></div>
        <section class="dv-timeline-panel" aria-label="Linear video and audio editor"><div class="dv-timeline-toolbar"><span class="dv-eyebrow">SEQUENCE EDITOR<span class="dv-timeline-detail"> · 3 VIDEO / 4 AUDIO</span></span><button data-action="import">Import</button><input id="dv-import" type="file" accept="video/mp4,video/quicktime,video/webm,audio/mpeg,audio/wav,audio/mp4,audio/aac,audio/ogg" multiple hidden><button data-action="split">Split</button><button data-action="delete">Delete</button>${selectedClip()?`<label class="dv-track-assign">Layer <select id="dv-track-assign" aria-label="Move selected clip to layer">${state.tracks.filter(t=>t.type===selectedClip().type).map(t=>`<option value="${t.id}" ${t.id===selectedClip().trackId?'selected':''}>${esc(t.name)}</option>`).join('')}</select></label>`:''}${selectedClip()?.type==='audio'?`<label class="dv-volume">Volume <input id="dv-volume" type="range" min="0" max="2" step=".05" value="${selectedClip().volume??1}" aria-label="Selected clip volume"></label>`:''}<span class="dv-zoom"><button data-action="zoom-out" aria-label="Zoom out timeline">−</button><input id="dv-zoom" type="range" min=".5" max="3" step=".25" value="${state.zoom}" aria-label="Timeline zoom"><button data-action="zoom-in" aria-label="Zoom in timeline">+</button></span><output id="dv-time">00:00.0</output></div><div class="dv-ruler"><div class="dv-ruler-scroll" id="dv-ruler-scroll"><div class="dv-scale" id="dv-scale"></div></div></div><div class="dv-tracks" id="dv-tracks"></div></section>
      </main><div id="dv-notice" class="dv-notice" role="status" aria-live="polite"></div></div>`;
  }
  function captureDrafts(){if(!root)return;const chat=$('#dv-chat-input',root),mobilePrompt=$('#dv-single-prompt',root);if(chat)state.drafts.chat=chat.value;if(mobilePrompt)state.drafts.mobilePrompt=mobilePrompt.value;const map={ 'dv-mobile-mode':'mode','dv-mobile-duration':'duration','dv-mobile-frames':'frameImages','dv-mobile-ref-images':'referenceImages','dv-mobile-ref-videos':'referenceVideos','dv-mobile-ref-audios':'referenceAudios','dv-mobile-source':'sourceVideo','dv-mobile-video-id':'videoId','dv-mobile-shots':'shots' };for(const [id,key] of Object.entries(map)){const node=$(`#${id}`,root);if(node)state.drafts.mobileMedia[key]=node.value;}const audio=$('#dv-mobile-audio',root);if(audio)state.drafts.mobileMedia.generateAudio=audio.checked;}
  // Screenplay input events synchronously update state. Never recapture the soon-to-be-replaced
  // textarea here: an authoritative server adoption may have just advanced both text and revision.

  function render(capture=true) { if (!root) return; if(capture)captureDrafts(); const script=state.screenplay; root.innerHTML=baseMarkup(); const scriptNode=$('#dv-script',root),count=$('#dv-script-count',root);if(scriptNode)scriptNode.value=script;if(count)count.textContent=`${tokenEstimate(script).toLocaleString()} / 115,000 tokens`; applyControls(); paintTracks(); paintScenes(); paintPanels(); paintStatus(); bind(); }
  function paintScenes() {
    const box=$('#dv-scenes',root),strip=$('#dv-preview-strip',root);
    const visual=(s,i,className='dv-thumb')=>{
      const clip=state.clips.find(item=>item.sceneId===s.id&&item.type==='video'&&item.src);
      const source=s.thumbnail||s.thumbnailUrl||s.poster||(Array.isArray(s.frameImages)&&s.frameImages[0])||(Array.isArray(s.referenceImages)&&s.referenceImages[0])||clip?.src||'';
      const isVideo=!!source&&((clip?.src&&clip.src===source)||/\.(?:mp4|mov|webm)(?:[?#]|$)/i.test(source));
      const media=source?(isVideo?`<video src="${esc(source)}" muted playsinline preload="metadata" aria-hidden="true"></video>`:`<img src="${esc(source)}" loading="lazy" alt="">`):'<span class="dv-visual-empty" aria-hidden="true">◇</span>';
      return `<span class="${className}">${media}<b class="dv-shot-number">${String(i+1).padStart(2,'0')}</b></span>`;
    };
    if(box)box.innerHTML=state.scenes.length?state.scenes.map((s,i)=>`<article class="dv-scene ${s.id===state.selected?'selected':''}" data-scene="${s.id}"><button class="dv-scene-main">${visual(s,i)}<span class="dv-scene-copy"><b>${esc(s.title||`Scene ${i+1}`)}</b><small>${esc(s.prompt||'Awaiting direction')}</small></span></button><div class="dv-scene-controls"><select data-scene-model="${s.id}" aria-label="Model for ${esc(s.title||`scene ${i+1}`)}"><option value="">Project default</option>${modelOptions(s.model)}</select><button class="dv-scene-edit" data-action="edit-scene" data-scene-id="${s.id}" title="Edit scene" aria-label="Edit ${esc(s.title||`scene ${i+1}`)}">✎</button></div></article>`).join(''):'<p class="dv-empty-note">Your storyboard is empty. Add a scene or let the creative director make a first pass.</p>';
    if(strip)strip.innerHTML=state.scenes.length?state.scenes.map((s,i)=>`<button class="dv-preview-card ${s.id===state.selected?'selected':''}" data-scene="${s.id}" aria-current="${s.id===state.selected?'true':'false'}">${visual(s,i,'dv-preview-visual')}<span>${esc(s.title||`Scene ${i+1}`)}</span></button>`).join(''):'<span class="dv-preview-empty">Scene previews will appear here as the storyboard takes shape.</span>';
    const count=$('#dv-scene-count',root);if(count)count.textContent=state.scenes.length;
  }
  function applyControls() { [['#dv-platform','platform'],['#dv-ratio','ratio'],['#dv-resolution','resolution'],['#dv-format','format']].forEach(([sel,key]) => { const el=$(sel,root); if (el) el.value=state.project[key]; }); }
  function paintStatus() { const c=$('#dv-checkpoint',root); if(c) c.textContent=state.checkpoint; const t=$('#dv-time',root); if(t) t.textContent=`00:${String(Math.floor(state.playhead)).padStart(2,'0')}.${Math.round(state.playhead%1*10)}`; const e=$('#dv-estimate',root); if(e) e.textContent=state.job ? state.job.message : (state.scenes.length ? `Plan: ${state.scenes.length} scenes · estimate available before run` : 'Estimated after plan'); }
  function paintTracks() { const tracks=$('#dv-tracks',root), scale=$('#dv-scale',root); if(!tracks)return; const px=Math.round(80*state.zoom), duration=Math.max(30,state.project.duration||30,...state.clips.map(c=>c.start+c.duration)); scale.style.width=`${Math.ceil(duration)*px}px`; scale.innerHTML=Array.from({length:Math.ceil(duration/5)+1},(_,i)=>`<span style="left:${i*5*px}px">${String(i*5).padStart(2,'0')}s</span>`).join(''); tracks.innerHTML=state.tracks.map(track=>`<div class="dv-track" data-track="${track.id}"><div class="dv-track-controls"><b>${esc(track.name)}</b><button data-track-action="mute" aria-label="Mute ${esc(track.name)}" aria-pressed="${track.mute}">M</button><button data-track-action="solo" aria-label="Solo ${esc(track.name)}" aria-pressed="${track.solo}">S</button><button data-track-action="lock" aria-label="Lock ${esc(track.name)}" aria-pressed="${track.lock}">⌑</button></div><div class="dv-lane" style="width:${Math.ceil(duration)*px}px">${state.clips.filter(c=>c.trackId===track.id).map(c=>`<button class="dv-clip ${c.id===state.selected?'selected':''} ${c.type}" data-clip="${c.id}" aria-label="${esc(c.name)}, ${c.duration} seconds on ${esc(track.name)}" style="left:${c.start*px}px;width:${Math.max(36,c.duration*px)}px"><i class="dv-trim left" aria-hidden="true"></i><span>${esc(c.name)}</span><small>${c.linked?'↗ linked':''}</small><i class="dv-trim right" aria-hidden="true"></i></button>`).join('')}<i class="dv-playhead" style="left:${state.playhead*px}px"></i></div></div>`).join(''); }
  function paintPanels() { ['writer','board'].forEach(p=> { const node=$(`#dv-${p==='writer'?'writer':'board'}`,root); node.classList.remove('expanded','minimized'); node.classList.add(state.panels[p]); const expand=$('[data-action="panel-expand"]',node),isExpanded=state.panels[p]==='expanded';if(expand){expand.textContent=isExpanded?'↙':'↗';expand.setAttribute('aria-label',`${isExpanded?'Collapse':'Expand'} ${p==='writer'?'screenwriter':'storyboard'}`);} }); const modes=Object.values(state.panels),expanded=modes.filter(x=>x==='expanded').length,regular=modes.filter(x=>x==='regular').length; root.classList.toggle('dv-two-expanded',expanded===2); root.classList.toggle('dv-has-expanded',expanded>0); root.classList.toggle('dv-no-regular',regular===0);root.classList.toggle('dv-one-regular',regular===1);root.classList.toggle('dv-writer-minimized',state.panels.writer==='minimized');root.classList.toggle('dv-board-minimized',state.panels.board==='minimized');root.classList.toggle('dv-focus-mode',!!state.focus); const focus=$('#dv-focus',root); if(focus) focus.textContent=state.focus?'↙ Exit player + editor':'⛶ Player + editor'; const tray=$('#dv-tray',root),minimized=['writer','board'].filter(p=>state.panels[p]==='minimized'); if(tray){tray.classList.toggle('empty',!minimized.length);tray.innerHTML=minimized.length?minimized.map(p=>`<button data-action="panel-restore" data-panel="${p}">${p==='writer'?'✦ Screenwriter':'◫ Storyboard'}</button>`).join(''):'<span class="dv-tray-label">Panel tray</span>';} }
  function constrainModel(model,{announce=false}={}){const cap=capability(model);if(!cap)return false;const changed=[];if(!cap.ratios.includes(state.project.ratio)){state.project.ratio=cap.ratios[0];changed.push(`ratio ${state.project.ratio}`);}if(!cap.resolutions.includes(state.project.resolution)){state.project.resolution=cap.resolutions[0];changed.push(`resolution ${state.project.resolution}`);}if(!cap.modes.includes(state.drafts.mobileMedia.mode)){state.drafts.mobileMedia.mode=cap.modes[0];changed.push(`mode ${cap.modes[0]}`);}const duration=Number(state.drafts.mobileMedia.duration);if(!Number.isInteger(duration)||duration<cap.minDuration||duration>cap.maxDuration){state.drafts.mobileMedia.duration=cap.minDuration;changed.push(`duration ${cap.minDuration}s`);}if(announce&&changed.length)toast(`${modelLabel(model)} does not support the prior settings; changed to ${changed.join(', ')}.`,'error');return true;}
  function bind() {
    if(state.screenwriterRecovery?.actionRequired){const button=document.createElement('button'),safe=['retry_settlement','check_generation','retry_status','quarantine_unrecoverable'].includes(state.screenwriterRecovery.recoveryAction);button.type='button';button.className='dv-reconcile';button.textContent=state.screenwriterRecovery.recoveryAction==='retry_status'?'Check Trinity status':state.screenwriterRecovery.recoveryAction==='quarantine_unrecoverable'?'Review unrecoverable turn':safe?'Resolve saved Trinity turn':'Operator reconciliation required';button.title=state.screenwriterRecovery.message||button.textContent;button.disabled=!safe;button.setAttribute('aria-disabled',String(!safe));button.onclick=reconcileScreenwriterNow;$('#dv-writer footer',root)?.append(button);}
    root.onclick=e=> { const a=e.target.closest('[data-action]'); if(a){ e.preventDefault(); action(a.dataset.action,a.dataset); return; } const scene=e.target.closest('[data-scene]'); if(scene && !e.target.closest('select')){ state.selected=scene.dataset.scene; render(); } const clip=e.target.closest('[data-clip]'); if(clip){ state.selected=clip.dataset.clip; render(); } const tb=e.target.closest('[data-track-action]'); if(tb){ const tr=tb.closest('.dv-track'); mutate('Updated track control',()=>tr && (state.tracks.find(x=>x.id===tr.dataset.track)[tb.dataset.trackAction]=!state.tracks.find(x=>x.id===tr.dataset.track)[tb.dataset.trackAction])); } };
    root.oninput=e=> { const field={ 'dv-project-name':'name','dv-model':'model','dv-purpose':'purpose','dv-platform':'platform','dv-ratio':'ratio','dv-resolution':'resolution','dv-format':'format' }[e.target.id],editLocked=!!projectSwitching||projectEditLocked();if(editLocked&&(field||e.target.id==='dv-volume'||e.target.id==='dv-script')){toast('Project editing is paused until the current durable operation finishes or reconciles.','error');render(false);return;} if(field) { state.project[field]=e.target.value;editRevision++; save(); if(['ratio','resolution'].includes(field)) paintStatus(); } if(e.target.id==='dv-chat-input')state.drafts.chat=e.target.value;if(e.target.id==='dv-single-prompt')state.drafts.mobilePrompt=e.target.value;if(e.target.id==='dv-script'){ state.screenplay=e.target.value;editRevision++; const tokens=tokenEstimate(e.target.value);$('#dv-script-count',root).textContent=`${tokens.toLocaleString()} / 115,000 tokens`;clearTimeout(scriptSaveTimer);if(tokens<=115000)scriptSaveTimer=setTimeout(()=>checkpoint('Edited screenplay'),1500); } if(e.target.id==='dv-zoom'){ state.zoom=Number(e.target.value);save();paintTracks(); } if(e.target.id==='dv-volume'){const clip=selectedClip();if(clip){clip.volume=Number(e.target.value);editRevision++;}} };
    root.onchange=e=> { const field={ 'dv-project-name':'name','dv-model':'model','dv-purpose':'purpose','dv-platform':'platform','dv-ratio':'ratio','dv-resolution':'resolution','dv-format':'format' }[e.target.id],editLocked=!!projectSwitching||projectEditLocked();if(editLocked&&(field||e.target.dataset.sceneModel||['dv-track-assign','dv-volume'].includes(e.target.id))){render(false);return;} if(field==='model'){captureDrafts();constrainModel(state.project.model,{announce:true});render(false);} if(field) checkpoint(`Updated ${field}`); if(e.target.dataset.sceneModel){ const scene=state.scenes.find(x=>x.id===e.target.dataset.sceneModel); if(scene) mutate('Changed scene model',()=>scene.model=e.target.value||null); } if(e.target.id==='dv-track-assign'){const clip=selectedClip();if(clip)mutate('Moved clip to layer',()=>clip.trackId=e.target.value);} if(e.target.id==='dv-zoom'){if(editLocked)save();else checkpoint('Changed timeline zoom');} if(e.target.id==='dv-volume')checkpoint('Adjusted audio volume');if(e.target.id?.startsWith('dv-mobile-')){captureDrafts();if(e.target.id==='dv-mobile-mode'||e.target.id==='dv-mobile-audio')render();} };
    const chatForm=$('#dv-chat-form',root),importInput=$('#dv-import',root);if(chatForm)chatForm.onsubmit=e=>{e.preventDefault(); sendChat();};if(importInput)importInput.onchange=e=>importMedia([...e.target.files]);
    $$('.dv-clip',root).forEach(clip=>clip.addEventListener('pointerdown',beginDrag));
    $$('.dv-lane',root).forEach(lane=>lane.addEventListener('pointerdown',e=>{if(e.target.closest('.dv-clip'))return;const rect=lane.getBoundingClientRect();state.playhead=Math.max(0,Math.round(((e.clientX-rect.left)/(80*state.zoom))*10)/10);paintTracks();paintStatus();}));
  }
  function beginDrag(e) { if(projectSwitching||projectEditLocked())return toast('Timeline edits are paused until the current durable operation finishes or reconciles.','error');const el=e.currentTarget, c=state.clips.find(x=>x.id===el.dataset.clip), lane=el.closest('.dv-lane'); if(!c || state.tracks.find(t=>t.id===c.trackId)?.lock) return; const edge=e.target.classList.contains('dv-trim') ? (e.target.classList.contains('left')?'left':'right') : 'move'; drag={id:c.id,edge,startX:e.clientX, original:clone(c), before:clone(projectPayload()), px:80*state.zoom, lane}; el.setPointerCapture?.(e.pointerId); document.addEventListener('pointermove',moveDrag); document.addEventListener('pointerup',endDrag,{once:true}); }
  function moveDrag(e) { if(!drag)return; const c=state.clips.find(x=>x.id===drag.id), d=(e.clientX-drag.startX)/drag.px; if(drag.edge==='move') c.start=Math.max(0,Math.round((drag.original.start+d)*10)/10); else if(drag.edge==='right') c.duration=Math.max(.5,Math.round((drag.original.duration+d)*10)/10); else { const end=drag.original.start+drag.original.duration, start=Math.max(0,Math.min(end-.5,drag.original.start+d)); c.duration=Math.round((end-start)*10)/10;c.start=Math.round(start*10)/10;c.sourceStart=Math.max(0,Math.round((Number(drag.original.sourceStart||0)+(start-drag.original.start))*10)/10); } paintTracks(); }
  function endDrag() { if(drag){if(projectSwitching||projectEditLocked()){const clip=state.clips.find(item=>item.id===drag.id);if(clip)Object.assign(clip,drag.original);render(false);}else{editRevision++;checkpoint('Adjusted timeline clip', drag.before);}drag=null;} document.removeEventListener('pointermove',moveDrag); }
  function action(name,data={}) { if(isMobileSingle() && ['dashboard','history','focus','import','zoom-in','zoom-out','panel-expand','panel-minimize','panel-restore','add-scene','edit-scene','delete','split','screenwrite','export','open-chat','mobile-nav'].includes(name)) return toast('This phone is in single-generation mode. Open Dominion on desktop for saved projects and editing.','error'); switch(name) { case 'close': close(); break; case 'conflict': conflictDialog();break; case 'dashboard': dashboard(); break; case 'history': historyDialog();break; case 'focus': mutate(state.focus?'Exited editor focus':'Entered editor focus',()=>state.focus=!state.focus); break; case 'undo': undo(); break; case 'redo': redo(); break; case 'play': {const video=$('#dv-preview-video',root);if(video){if(video.paused){video.play().catch(()=>{});state.playing=true;}else{video.pause();state.playing=false;}const b=$('[data-action="play"]',root);if(b)b.textContent=state.playing?'❚❚':'▶';}else toast('Select a generated video clip first.');break;} case 'import': if(projectSwitching||projectEditLocked())toast('Media import is paused until the current durable operation finishes or reconciles.','error');else $('#dv-import',root)?.click(); break; case 'zoom-in': mutate('Changed timeline zoom',()=>state.zoom=Math.min(3,state.zoom+.25)); break; case 'zoom-out': mutate('Changed timeline zoom',()=>state.zoom=Math.max(.5,state.zoom-.25)); break; case 'panel-expand': panel(data.panel,state.panels[data.panel]==='expanded'?'regular':'expanded'); break; case 'panel-minimize': panel(data.panel,'minimized'); break; case 'panel-restore': panel(data.panel,'regular'); break; case 'add-scene': addScene(); break; case 'edit-scene': editScene(data.sceneId); break; case 'delete': deleteSelected(); break; case 'split': splitSelected(); break; case 'generate': generate(); break; case 'screenwrite': screenwrite(); break; case 'export': exportProject(); break; case 'open-chat': $('#dv-chat-input',root)?.focus(); break; case 'mobile-nav': mobileNav(); break; } }
  function panel(id,mode) { if(!['writer','board'].includes(id))return; mutate(`${mode==='minimized'?'Minimized':mode==='expanded'?'Expanded':'Restored'} ${id}`,()=>state.panels[id]=mode); }
  function addScene(data={}) { if(state.scenes.length>=100) return toast('A project can contain at most 100 storyboard scenes.','error'); mutate('Added storyboard scene',()=>{ const n=state.scenes.length+1, id=uid(); state.scenes.push({id,title:data.title||`Scene ${n}`,prompt:data.prompt||'',model:null,status:'draft'}); state.selected=id; }); }
  function editScene(id){
    const scene=state.scenes.find(s=>s.id===id);if(!scene)return;const model=scene.model||state.project.model,cap=capability(model);if(!cap)return toast('That scene model is unavailable.','error');
    const mode=cap.modes.includes(scene.mode)?scene.mode:(scene.frameImages?.length&&cap.modes.includes('image')?'image':cap.modes[0]);
    const field=(label,input)=>`<label class="dv-modal-field">${label}${input}</label>`;
    const html=`<header><span class="dv-eyebrow">SCENE DIRECTION · ${esc(modelLabel(model))}</span><button data-modal-close aria-label="Close scene editor">×</button></header><h2>Edit ${esc(scene.title||'scene')}</h2>
      ${field('Title',`<input id="dv-scene-title" maxlength="160" value="${esc(scene.title||'')}">`)}${field('Generation prompt',`<textarea id="dv-scene-prompt" maxlength="${cap.promptMax||32000}" rows="6">${esc(scene.prompt||'')}</textarea>`)}
      <div class="dv-generation-grid">${field('Mode',`<select id="dv-scene-mode">${options(cap.modes,mode)}</select>`)}${field(`Clip seconds (${cap.minDuration}–${cap.maxDuration})`,`<input id="dv-scene-duration" type="number" min="${cap.minDuration}" max="${cap.maxDuration}" step="1" value="${Number(scene.duration||cap.minDuration)}">`)}
      ${cap.maxFrameImages?field(`Frame image URLs · max ${cap.maxFrameImages}`,`<textarea id="dv-scene-frames" rows="3">${esc(lines(scene.frameImages))}</textarea>`):''}${cap.maxReferenceImages?field(`Reference image URLs · max ${cap.maxReferenceImages}${cap.maxEditReferenceImages?`, edit/continue max ${cap.maxEditReferenceImages}`:''}`,`<textarea id="dv-scene-ref-images" rows="3">${esc(lines(scene.referenceImages))}</textarea>`):''}${cap.maxReferenceVideos?field(`Reference video URLs · max ${cap.maxReferenceVideos}`,`<textarea id="dv-scene-ref-videos" rows="3">${esc(lines(scene.referenceVideos))}</textarea>`):''}${cap.maxReferenceAudio?field(`Reference audio URLs · max ${cap.maxReferenceAudio}`,`<textarea id="dv-scene-ref-audios" rows="3">${esc(lines(scene.referenceAudios))}</textarea>`):''}${cap.modes.includes('edit')?field('Source video URL',`<input id="dv-scene-source" value="${esc(scene.sourceVideo||'')}">`):''}${cap.modes.includes('continue')?field('Prior video ID',`<input id="dv-scene-video-id" value="${esc(scene.videoId||'')}">`):''}${cap.maxShots?field(`Kling shots JSON · max ${cap.maxShots}`,`<textarea id="dv-scene-shots" rows="4" placeholder='[{"duration":3,"prompt":"Wide shot"}]'>${esc(scene.shots?.length?JSON.stringify(scene.shots,null,2):'')}</textarea>`):''}<label class="dv-check"><input id="dv-scene-audio" type="checkbox" ${scene.generateAudio!==false?'checked':''}> Generate audio</label></div><button class="dv-primary dv-modal-save">Save scene checkpoint</button>`;
    dialog(html,(d,closeD)=>{$('.dv-modal-save',d).onclick=()=>{try{const prompt=$('#dv-scene-prompt',d).value.trim(),duration=Number($('#dv-scene-duration',d).value),selectedMode=$('#dv-scene-mode',d).value;const get=id=>mediaLines($(`#${id}`,d)?.value);if(prompt.length<(cap.promptMin||1)||prompt.length>(cap.promptMax||32000))throw new Error(`Prompt must contain ${cap.promptMin||1} to ${cap.promptMax||32000} characters.`);if(!Number.isInteger(duration)||duration<cap.minDuration||duration>cap.maxDuration)throw new Error(`Duration must be a whole number from ${cap.minDuration} to ${cap.maxDuration} seconds.`);if(!cap.modes.includes(selectedMode))throw new Error('That generation mode is not supported.');const frameImages=get('dv-scene-frames'),referenceImages=get('dv-scene-ref-images'),referenceVideos=get('dv-scene-ref-videos'),referenceAudios=get('dv-scene-ref-audios'),maxReferenceImages=['edit','continue'].includes(selectedMode)?Number(cap.maxEditReferenceImages??cap.maxReferenceImages??0):Number(cap.maxReferenceImages||0);for(const [items,max,label] of [[frameImages,cap.maxFrameImages||0,'frame images'],[referenceImages,maxReferenceImages,'reference images'],[referenceVideos,cap.maxReferenceVideos||0,'reference videos'],[referenceAudios,cap.maxReferenceAudio||0,'reference audio']])if(items.length>max)throw new Error(`Too many ${label}; ${modelLabel(model)} allows ${max}.`);let shots=[];const shotsText=$('#dv-scene-shots',d)?.value.trim();if(shotsText){shots=JSON.parse(shotsText);if(!Array.isArray(shots))throw new Error('Kling shots must be a JSON array.');}mutate('Updated storyboard scene',()=>Object.assign(scene,{title:$('#dv-scene-title',d).value.trim()||scene.title,prompt,duration,mode:selectedMode,frameImages,referenceImages,referenceVideos,referenceAudios,sourceVideo:$('#dv-scene-source',d)?.value.trim()||'',videoId:$('#dv-scene-video-id',d)?.value.trim()||'',generateAudio:$('#dv-scene-audio',d)?.checked!==false,shots}));closeD();}catch(error){toast(readable(error),'error');}};},'Scene editor');
  }
  function selectedClip(){return state.clips.find(c=>c.id===state.selected);}
  function deleteSelected(){ const c=selectedClip(); if(c) mutate('Deleted timeline clip',()=>{state.clips=state.clips.filter(x=>x.id!==c.id && x.linked!==c.id);state.selected=null;}); else { const s=state.scenes.find(s=>s.id===state.selected); if(!s)return toast('Select a scene or a timeline clip first.'); mutate('Deleted storyboard scene',()=>{state.scenes=state.scenes.filter(x=>x.id!==s.id);state.selected=null;}); } }
  function splitSelected(){const clip=selectedClip();if(!clip)return toast('Select a timeline clip to split.');const splitAt=Number(state.playhead),start=Number(clip.start),end=start+Number(clip.duration);if(!(splitAt>start+.1&&splitAt<end-.1))return toast('Move the playhead inside the selected clip, away from either edge.','error');mutate('Split linked timeline media',()=>{const linked=clip.linked?state.clips.find(item=>item.id===clip.linked):null;const splitOne=item=>{const left=Math.round((splitAt-item.start)*10)/10,right=Math.round((item.duration-left)*10)/10;if(left<=.1||right<=.1)return null;const next={...clone(item),id:uid(),name:`${item.name} — part 2`,start:splitAt,sourceStart:Math.round((Number(item.sourceStart||0)+left)*10)/10,duration:right,linked:null};item.duration=left;state.clips.push(next);return next;};const right=splitOne(clip),linkedRight=linked&&splitAt>linked.start+.1&&splitAt<linked.start+linked.duration-.1?splitOne(linked):null;if(right&&linkedRight){right.linked=linkedRight.id;linkedRight.linked=right.id;}state.selected=right?.id||clip.id;});}
  async function importMedia(files){
    if(isMobileSingle())return toast('Import is available when a desktop session is running.','error');if(!files.length)return;if(projectSwitching||projectEditLocked())return toast('Media import is paused until the current durable operation finishes.','error');const epoch=checkpointEpoch;let projectId=null;state.inflight.import=true;
    try{projectId=await ensureProject();if(!sameSession(projectId,epoch))return;for(const file of files){toast(`Importing and verifying ${file.name}…`);const res=await fetch(`${API}/uploads?projectId=${encodeURIComponent(projectId)}&filename=${encodeURIComponent(file.name)}`,{method:'POST',credentials:'same-origin',headers:{'Content-Type':file.type||'application/octet-stream','X-Dominion-Device':device(),'X-Dominion-Filename':file.name},body:file});let data;try{data=await res.json();}catch{data=null;}if(!res.ok)throw new Error(typeof data?.error==='string'?data.error:data?.error?.message||`Import failed with ${res.status}.`);if(!sameSession(projectId,epoch))return;const start=state.clips.reduce((max,clip)=>Math.max(max,clip.start+clip.duration),0),duration=Math.max(.1,Number(data.duration)||1),videoId=uid();if(data.hasVideo)state.clips.push({id:videoId,trackId:'v1',name:file.name,start,duration,sourceStart:0,type:'video',linked:null,mediaFile:data.filename,src:data.mediaUrl,volume:1,fit:'contain'});if(data.hasAudio){const audioId=uid(),video=state.clips.find(c=>c.id===videoId);state.clips.push({id:audioId,trackId:'a1',name:`${file.name} audio`,start,duration,sourceStart:0,type:'audio',linked:data.hasVideo?videoId:null,mediaFile:data.filename,src:data.mediaUrl,volume:1});if(video)video.linked=audioId;}state.selected=data.hasVideo?videoId:state.clips.at(-1)?.id;editRevision++;await checkpoint(`Imported ${file.name}`);render();}toast('Imported media is verified and ready to edit.');}catch(error){if(state.open&&checkpointEpoch===epoch){render();toast(readable(error),'error');}}finally{if(state.open&&checkpointEpoch===epoch){state.inflight.import=false;render(false);}}
  }
  function buildGenerationRequest(scene,single){const model=scene.model||state.project.model,cap=capability(model);if(!cap)throw Object.assign(new Error('The selected video model is unavailable.'),{code:'video_model_unsupported'});const mobile=state.drafts.mobileMedia;const source=single?{mode:mobile.mode,duration:Number(mobile.duration),frameImages:mediaLines(mobile.frameImages),referenceImages:mediaLines(mobile.referenceImages),referenceVideos:mediaLines(mobile.referenceVideos),referenceAudios:mediaLines(mobile.referenceAudios),sourceVideo:mobile.sourceVideo,videoId:mobile.videoId,generateAudio:mobile.generateAudio,shots:mobile.shots}:scene;const mode=String(source.mode||(source.frameImages?.length?'image':'text')),duration=Number(source.duration||cap.minDuration),ratio=scene.ratio||state.project.ratio,resolution=scene.resolution||state.project.resolution;if(!cap.modes.includes(mode))throw Object.assign(new Error(`${modelLabel(model)} does not support ${mode} mode.`),{code:'video_mode_unsupported'});if(!Number.isInteger(duration)||duration<cap.minDuration||duration>cap.maxDuration)throw Object.assign(new Error(`Duration must be a whole number from ${cap.minDuration} to ${cap.maxDuration} seconds.`),{code:'video_duration_invalid'});if(!cap.ratios.includes(ratio))throw Object.assign(new Error(`${modelLabel(model)} does not support ratio ${ratio}.`),{code:'video_ratio_invalid'});if(!cap.resolutions.includes(resolution))throw Object.assign(new Error(`${modelLabel(model)} does not support resolution ${resolution}.`),{code:'video_resolution_invalid'});const frameImages=Array.isArray(source.frameImages)?source.frameImages:[],referenceImages=Array.isArray(source.referenceImages)?source.referenceImages:[],referenceVideos=Array.isArray(source.referenceVideos)?source.referenceVideos:[],referenceAudios=Array.isArray(source.referenceAudios)?source.referenceAudios:[],maxReferenceImages=['edit','continue'].includes(mode)?Number(cap.maxEditReferenceImages??cap.maxReferenceImages??0):Number(cap.maxReferenceImages||0);for(const [items,max,label] of [[frameImages,cap.maxFrameImages||0,'frame images'],[referenceImages,maxReferenceImages,'reference images'],[referenceVideos,cap.maxReferenceVideos||0,'reference videos'],[referenceAudios,cap.maxReferenceAudio||0,'reference audio']])if(items.length>max)throw Object.assign(new Error(`${modelLabel(model)} allows at most ${max} ${label}.`),{code:'video_media_limit'});if(mode==='image'&&!frameImages.length)throw Object.assign(new Error('Image mode requires a starting frame URL.'),{code:'video_image_required'});if(mode==='reference'&&!referenceImages.length&&!referenceVideos.length)throw Object.assign(new Error('Reference mode requires at least one reference image or video URL.'),{code:'video_reference_required'});if(mode==='edit'&&!String(source.sourceVideo||'').trim())throw Object.assign(new Error('Edit mode requires a source video URL.'),{code:'video_source_required'});if(mode==='continue'&&!String(source.videoId||'').trim())throw Object.assign(new Error('Continue mode requires a prior video ID.'),{code:'video_id_required'});let shots=source.shots||[];if(typeof shots==='string'&&shots.trim())try{shots=JSON.parse(shots);}catch{throw Object.assign(new Error('Kling shots must be valid JSON.'),{code:'video_shots_invalid'});}if(!Array.isArray(shots))shots=[];if(shots.length){const total=shots.reduce((sum,item)=>sum+Number(item?.duration||0),0);if(model!=='klingai:kling-video@3.0-turbo'||shots.length>(cap.maxShots||0)||total!==duration)throw Object.assign(new Error(`Kling shots must be within the model limit and total exactly ${duration} seconds.`),{code:'video_shots_invalid'});}return{model,mode,duration,ratio,resolution,prompt:String(scene.prompt||'').trim(),frameImages,referenceImages,referenceVideos,referenceAudios,sourceVideo:String(source.sourceVideo||'').trim(),videoId:String(source.videoId||'').trim(),generateAudio:source.generateAudio!==false,shots,privacyMode:currentPrivacyMode(),idempotencyKey:`gen-${globalThis.crypto?.randomUUID?.()||uid()}`};}
  async function generate(){
    if(generationBusy())return toast('A video generation is already active.','error');captureDrafts();const single=isMobileSingle();if(!single&&(projectSwitching||projectEditLocked()))return toast('Video generation is paused until the current durable project operation finishes or reconciles.','error');if(!single&&!state.scenes.length)addScene();const scene=single?{id:null,prompt:state.drafts.mobilePrompt,model:state.project.model}:state.scenes.find(item=>item.id===state.selected)||state.scenes[0];if(!scene||!String(scene.prompt||'').trim())return single?toast('Describe the video you want to generate.','error'):editScene(scene?.id);let generation;try{generation=buildGenerationRequest(scene,single);}catch(error){toast(readable(error),'error');return;}const hadScriptSave=!!scriptSaveTimer;clearTimeout(scriptSaveTimer);scriptSaveTimer=null;const epoch=checkpointEpoch;let projectId=null,generationSubmitted=false;state.inflight.generation=true;state.job={message:'Validating generation request…'};render();
    try{if(!single){projectId=await ensureProject();if(hadScriptSave||editRevision>savedRevision)await checkpoint('Saved edits before video generation');await flushCheckpoints();if(!sameSession(projectId,epoch))return;}generationSubmitted=true;const data=await request('/jobs',{method:'POST',body:JSON.stringify({projectId:single?null:projectId,sceneId:scene.id,purpose:state.project.purpose,platform:state.project.platform,...generation})});if(!single&&!sameSession(projectId,epoch))return;const id=data.id||data.jobId;if(!id)throw new Error('The generation service did not return a job id.');state.job={id,projectId:data.projectId,singleGeneration:!!data.singleGeneration,message:data.message||'Generation queued.'};activeJobs.set(id,{projectId:data.projectId,singleGeneration:!!data.singleGeneration,timer:null});state.inflight.generation=false;render();poll(id,data.projectId,!!data.singleGeneration);}catch(error){
      // A capability refusal after a redeploy heals itself; every other failure lands in the chat
      // log PERSISTENTLY (a 5-second toast is how a broken video service read as a mystery).
      if(error?.code==='desktop_capability_required')void refreshDesktopCapability('The video service restarted; this tab has reconnected. Press Generate again.');
      if(state.open&&checkpointEpoch===epoch)state.messages.push({role:'status',code:error?.code||'video_generation_failed',content:readable(error)});
      if(!single&&generationSubmitted&&projectId&&sameSession(projectId,epoch)){try{await refreshAuthoritativeProject(projectId,epoch);}catch{}state.inflight.generation=false;state.job=null;render();toast(readable(error),'error');}else if(single||state.open&&checkpointEpoch===epoch){state.inflight.generation=false;state.job=null;render();toast(readable(error),'error');}}
  }
  function mergeServerClips(serverClips, jobId, mediaUrl) { const incoming=serverClips.filter(c=>c&&['video','audio'].includes(c.type)).map(c=>({...clone(c),id:String(c.id||uid()),mediaJobId:c.mediaJobId||jobId,src:c.src||mediaUrl||null,sourceStart:Number(c.sourceStart||0),duration:Number(c.duration||0)})).filter(c=>c.duration>0); if(!incoming.length)return 0; for(const clip of incoming){const match=state.clips.find(existing=>(existing.id===clip.id)||(existing.type===clip.type&&existing.mediaJobId===clip.mediaJobId)); if(match)Object.assign(match,clip);else state.clips.push(clip);} render(); return incoming.length; }
  function sameOriginDownload(url,filename){const target=new URL(url,location.href);if(target.origin!==location.origin)throw Object.assign(new Error('Download URL was not same-origin.'),{code:'video_download_origin'});const link=document.createElement('a');link.href=target.pathname+target.search;link.download=filename;document.body.append(link);link.click();link.remove();const mobileMedia=target.pathname.match(/^\/api\/video\/media\/([^/]+)\/([^/]+)\.mp4$/);if(isMobileSingle()&&mobileMedia){const projectId=decodeURIComponent(mobileMedia[1]),jobId=decodeURIComponent(mobileMedia[2]);request(`/jobs/${encodeURIComponent(jobId)}/delivered`,{method:'POST',body:JSON.stringify({projectId})}).catch(error=>toast(`The video downloaded, but delivery acknowledgement will retry. ${readable(error)}`,'error'));}}
  async function refreshAuthoritativeProject(projectId,epoch=checkpointEpoch){const loaded=await request(`/projects/${encodeURIComponent(projectId)}`);if(!sameSession(projectId,epoch))return false;applyPayload(loaded.project?.project?loaded.project:loaded);save();return true;}
  async function poll(id,projectId=state.project.id,singleGeneration=false) {
    if(!id||(!singleGeneration&&projectId!==state.project.id)){activeJobs.delete(id);return;}if(!activeJobs.has(id))activeJobs.set(id,{projectId,singleGeneration,timer:null});const entry=activeJobs.get(id),epoch=checkpointEpoch;if(entry.polling)return;entry.polling=true;
    try{const j=await request(`/jobs/${encodeURIComponent(id)}?projectId=${encodeURIComponent(projectId)}`);entry.polling=false;if(!singleGeneration&&!sameSession(projectId,epoch)){activeJobs.delete(id);return;}state.job={id,projectId,singleGeneration,message:j.message||(`${j.status||'Generating'}${Number.isFinite(j.progress)?` · ${j.progress}%`:''}`)};const status=String(j.status||'').toLowerCase(),ready=['ready','completed','succeeded'].includes(status),failed=['failed','cancelled','rejected'].includes(status);if(ready||failed){if(!singleGeneration&&!await refreshAuthoritativeProject(projectId,epoch))return;activeJobs.delete(id);state.job=null;if(ready){if(singleGeneration){sameOriginDownload(j.mediaUrl,`dominion-video-${id}.mp4`);toast('Video verified and sent to your phone.');}else toast('Verified server clips were loaded from the authoritative project checkpoint.');render();return;}throw Object.assign(new Error(j.message||j.error||'The provider did not produce a usable video.'),{code:j.code||'video_generation_failed',terminal:true});}render();entry.timer=setTimeout(()=>{entry.timer=null;poll(id,projectId,singleGeneration);},Math.max(2000,j.retryAfterMs||3000));}
    catch(error){entry.polling=false;const transient=!error?.terminal&&(!Number.isInteger(Number(error?.status))||[408,429].includes(Number(error.status))||Number(error.status)>=500);if(transient){state.job={id,projectId,singleGeneration,message:`Generation is still saved; retrying status after ${readable(error)}`};render();entry.timer=setTimeout(()=>{entry.timer=null;poll(id,projectId,singleGeneration);},5000);return;}activeJobs.delete(id);state.job=null;if(state.open)state.messages.push({role:'status',code:error?.code||'video_generation_failed',content:readable(error)});render();toast(readable(error),'error');}
  }
  async function screenwrite(){
    if(!fullEditor())return toast('Screenwriting is available on desktop.','error');if(projectSwitching||state.inflight.chat||generationBusy()||state.inflight.import||state.inflight.export)return toast('Finish the current project action before starting Trinity.','error');if(state.inflight.screenwriter||state.screenwriterRecovery?.pending)return toast('The saved Trinity turn must finish or reconcile before another request.','error');const prompt=$('#dv-script',root)?.value||'';if(!prompt.trim())return toast('Add a story brief first.');const tokens=tokenEstimate(prompt);if(tokens>115000)return toast('Screenplay text is limited to 115,000 total context tokens.','error');clearTimeout(scriptSaveTimer);scriptSaveTimer=null;const epoch=checkpointEpoch;let turnProjectId=null,setup=null,providerPrepared=false;state.inflight.screenwriter=true;render();
    try{
      setup=(async()=>{turnProjectId=await ensureProject();if(!sameSession(turnProjectId,epoch))return false;await checkpoint('Saved screenplay before Trinity request');await flushCheckpoints();if(lastSaveFailure)throw lastSaveFailure;if(!sameSession(turnProjectId,epoch))return false;setScreenwriterRecovery(turnProjectId,{status:'active',message:'Trinity Large Thinking is preparing the next durable screenplay section.'});providerPrepared=true;render();return true;})();screenwriterSetup=setup;
      let ready;try{ready=await setup;}finally{if(screenwriterSetup===setup)screenwriterSetup=null;}if(!ready||!sameSession(turnProjectId,epoch))return;
      toast('Trinity Large Thinking is preparing the next saved section…');const d=await request('/screenwrite',{method:'POST',eventStream:true,body:JSON.stringify({projectId:turnProjectId,prompt,limit:115000,privacyMode:currentPrivacyMode()})});if(!sameSession(turnProjectId,epoch))return;if(d.projectId!==turnProjectId||!d.text||!/^[a-f0-9]{64}$/.test(String(d.screenplaySha256||'')))throw Object.assign(new Error('Dominion could not verify the saved Trinity result. The project will be refreshed before editing continues.'),{code:'screenwriter_result_unverified'});state.screenplay=d.text;state.screenplaySha256=d.screenplaySha256;setScreenwriterRecovery(turnProjectId,{status:'checking',message:'Loading the authoritative saved Trinity checkpoint.'});render();const refreshed=await reconcileScreenwriterProject(turnProjectId,{epoch,waitForTerminal:true});if(!refreshed||!sameSession(turnProjectId,epoch))throw Object.assign(new Error('Dominion could not reload the authoritative Trinity checkpoint. Editing remains paused until it can be verified.'),{code:'screenwriter_result_recovery_required'});state.checkpoint=d.truncated?'Saved partial screenplay section':'Saved screenplay section';try{await mirrorProject(projectPayload());}catch(error){toast(`Trinity checkpoint saved; folder mirror needs attention. ${readable(error)}`,'error');}toast(d.truncated?'Trinity reached its output limit. The partial section is appended and saved; ask it to continue from this checkpoint.':'Trinity screenplay checkpoint saved.',d.truncated?'error':'');
    }catch(error){if(providerPrepared&&turnProjectId&&sameSession(turnProjectId,epoch)){setScreenwriterRecovery(turnProjectId,{status:'checking',message:'Dominion is checking the authoritative saved Trinity turn before editing resumes.'});render();const refreshed=await reconcileScreenwriterProject(turnProjectId,{epoch,waitForTerminal:true});if(sameSession(turnProjectId,epoch))toast(`${readable(error)}${refreshed?' The project was refreshed from its authoritative server checkpoint.':state.screenwriterRecovery?.message?` ${state.screenwriterRecovery.message}`:''}`,'error');}else if(state.open&&checkpointEpoch===epoch)toast(readable(error),'error');}finally{if(screenwriterSetup===setup)screenwriterSetup=null;if(checkpointEpoch===epoch&&state.open&&(!turnProjectId||state.project.id===turnProjectId)){state.inflight.screenwriter=false;render();}}
  }
  async function sendChat(){
    if(!fullEditor())return toast('Liaison chat is available with a desktop session.','error');if(projectSwitching||projectEditLocked())return toast('Finish the current project action before sending a liaison message.','error');const input=$('#dv-chat-input',root),msg=input?.value.trim();if(!msg)return;const epoch=checkpointEpoch;let projectId=null;const hadScriptSave=!!scriptSaveTimer;clearTimeout(scriptSaveTimer);scriptSaveTimer=null;state.drafts.chat='';state.inflight.chat=true;render();
    try{projectId=await ensureProject();if(!sameSession(projectId,epoch))return;if(!writerStateLocked()&&(hadScriptSave||editRevision>savedRevision))await checkpoint('Saved edits before liaison request');await flushCheckpoints();if(!sameSession(projectId,epoch))return;const expectedProjectRevision=Number(state.projectRevision);if(!Number.isInteger(expectedProjectRevision))throw Object.assign(new Error('Refresh this project before sending a liaison request.'),{code:'video_project_revision_required'});state.messages.push({role:'user',content:msg});render();const d=await request('/chat',{method:'POST',body:JSON.stringify({projectId,expectedProjectRevision,message:msg,privacyMode:currentPrivacyMode(),context:{scenes:state.scenes.length,selected:state.selected}})});if(!sameSession(projectId,epoch))return;await flushCheckpoints();const loaded=await request(`/projects/${encodeURIComponent(projectId)}`);if(!sameSession(projectId,epoch))return;applyPayload(loaded.project?.project?loaded.project:loaded);if(d.visualOrchestrator?.available===false||d.visualOrchestrator?.error){const error=d.visualOrchestrator?.error||{};state.messages.push({role:'status',code:error.code||'visual_orchestrator_degraded',content:error.message||'The visual plan was unavailable; no substitute model was used.'});}const planned=d.visualOrchestrator?.applied===true&&Array.isArray(d.visualOrchestrator?.plan?.scenes)?d.visualOrchestrator.plan.scenes.length:0;if(planned)toast(`Saved visual plan refreshed ${planned} storyboard scene${planned===1?'':'s'}.`);else if(d.visualOrchestrator?.applyStatus==='quarantined_stale')toast('The AI plan was saved to history but did not overwrite newer storyboard edits.','error');}catch(error){if(projectId?sameSession(projectId,epoch):state.open&&checkpointEpoch===epoch){state.messages.push({role:'status',code:error.code||'video_chat_failed',content:error.message||readable(error)});toast(readable(error),'error');}}finally{if(projectId?sameSession(projectId,epoch):state.open&&checkpointEpoch===epoch){state.inflight.chat=false;render();}}
  }
  async function exportProject(){
    if(!fullEditor())return toast('Export is available with a desktop session.','error');if(projectSwitching||projectEditLocked())return toast('Export is paused until the current durable project operation finishes or reconciles.','error');const epoch=checkpointEpoch,hadScriptSave=!!scriptSaveTimer;let projectId=null,exportSubmitted=false;clearTimeout(scriptSaveTimer);scriptSaveTimer=null;state.inflight.export=true;render();
    try{projectId=await ensureProject();if(!sameSession(projectId,epoch))return;if(hadScriptSave||editRevision>savedRevision)await checkpoint('Saved edits before export');await flushCheckpoints();if(lastSaveFailure)throw lastSaveFailure;if(!sameSession(projectId,epoch))return;const snapshot={projectId,format:state.project.format,ratio:state.project.ratio,resolution:state.project.resolution,platform:state.project.platform,clips:clone(state.clips)},filename=`${state.project.name||'dominion-video'}.${state.project.format}`;toast('Checking export settings…');exportSubmitted=true;const d=await request('/exports',{method:'POST',body:JSON.stringify(snapshot)});if(!sameSession(projectId,epoch))return;if(!await refreshAuthoritativeProject(projectId,epoch))return;toast(d.message||'Export completed and verified.');if(d.url)sameOriginDownload(d.url,d.filename||filename);}catch(error){if(exportSubmitted&&projectId&&sameSession(projectId,epoch)){try{await refreshAuthoritativeProject(projectId,epoch);}catch{}toast(readable(error),'error');}else if(projectId?sameSession(projectId,epoch):state.open&&checkpointEpoch===epoch)toast(readable(error),'error');}finally{if(projectId?sameSession(projectId,epoch):state.open&&checkpointEpoch===epoch){state.inflight.export=false;render();}}
  }
  function trap(e,el){if(e.key!=='Tab')return;const f=$$('button,input,select,textarea,[tabindex]:not([tabindex="-1"])',el).filter(n=>!n.disabled); if(!f.length)return;const first=f[0],last=f.at(-1);if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus();}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus();}}
  function dialog(html,onBind,label='Video Studio dialog'){activeDialog?.close(false);const d=document.createElement('div'),returnFocus=document.activeElement;d.className='dv-modal';d.innerHTML=`<div class="dv-modal-card" role="dialog" aria-modal="true" aria-label="${esc(label)}">${html}</div>`;root.append(d);const closeD=(restore=true)=>{if(d.isConnected)d.remove();if(activeDialog?.node===d)activeDialog=null;if(restore)returnFocus?.focus?.();};activeDialog={node:d,close:closeD};d.addEventListener('click',e=>{if(e.target===d||e.target.closest('[data-modal-close]'))closeD();});d.addEventListener('keydown',e=>{if(e.key==='Escape'){e.preventDefault();e.stopPropagation();closeD();}else trap(e,d);});onBind?.(d,closeD);setTimeout(()=>$('input,button,select,textarea',d)?.focus(),0);return d;}
  function downloadConflictDraft(){if(!conflictDraft)return false;const safe=String(conflictDraft.snapshot?.project?.name||'dominion-video').replace(/[^A-Za-z0-9._-]+/g,'-').slice(0,80)||'dominion-video',payload={schemaVersion:1,kind:'dominion-video-save-conflict',capturedAt:conflictDraft.capturedAt,projectId:conflictDraft.projectId,label:conflictDraft.label,localDraft:conflictDraft.snapshot},url=URL.createObjectURL(new Blob([JSON.stringify(payload,null,2)],{type:'application/json'})),link=document.createElement('a');link.href=url;link.download=`${safe}-local-conflict-${Date.now()}.dominion-video.json`;document.body.append(link);link.click();link.remove();setTimeout(()=>URL.revokeObjectURL(url),1000);return true;}
  async function reloadAfterConflict(closeD){const held=conflictDraft;if(!held)return;const projectId=held.projectId,epoch=checkpointEpoch,loaded=await request(`/projects/${encodeURIComponent(projectId)}`);if(!sameSession(projectId,epoch)||conflictDraft!==held)return;applyPayload(loaded.project?.project?loaded.project:loaded);conflictDraft=null;lastSaveFailure=null;saveQueue=Promise.resolve();state.checkpoint='Loaded newer server checkpoint';save();closeD?.(false);render(false);toast('The newer server project is loaded. Your local draft was downloaded first.');}
  function conflictDialog(){if(!conflictDraft)return;dialog(`<header><span class="dv-eyebrow">SAVE CONFLICT</span><button data-modal-close aria-label="Close save conflict">×</button></header><h2>Your local draft is held</h2><p>Another tab saved a newer project checkpoint. Dominion did not overwrite it. Download this complete local draft before loading the server copy.</p><div class="dv-project-actions"><button class="dv-primary" data-conflict-download>Download local draft</button><button data-conflict-reload disabled>Load newer server copy</button></div><p data-conflict-status>The server project remains untouched and editing is paused.</p>`,(d,closeD)=>{const download=$('[data-conflict-download]',d),reload=$('[data-conflict-reload]',d),status=$('[data-conflict-status]',d);download.onclick=()=>{if(downloadConflictDraft()){reload.disabled=false;status.textContent='Local draft downloaded. You can now load the newer server checkpoint safely.';}};reload.onclick=async()=>{reload.disabled=true;download.disabled=true;status.textContent='Loading the authoritative server checkpoint…';try{await reloadAfterConflict(closeD);}catch(error){download.disabled=false;reload.disabled=false;status.textContent=`The server copy could not be loaded: ${readable(error)}`;}};},'Resolve video save conflict');}
  function screenwriterQuarantineDialog(){
    const recovery=state.screenwriterRecovery,projectId=state.project.id,epoch=checkpointEpoch,required=String(recovery?.quarantineConfirmation||''),reference=String(recovery?.operatorReference||'unavailable');
    if(!projectId||!required)return toast('Refresh the Trinity status before reviewing this unrecoverable turn.','error');
    dialog(`<header><span class="dv-eyebrow">TRINITY RECOVERY</span><button data-modal-close aria-label="Close Trinity recovery">×</button></header><h2>Preserve an unrecoverable turn</h2><p>Dominion could not safely verify and settle this OpenRouter response. Dominion will keep the attempt in project history, will not bill it to your Dominion balance, and will unlock editing. This does not recover the lost provider response.</p><p><b>Reference:</b> ${esc(reference)}</p><label class="dv-modal-field">Type this exact confirmation:<small>${esc(required)}</small><input data-quarantine-confirm autocomplete="off"></label><div class="dv-project-actions"><button class="dv-primary" data-quarantine-apply disabled>Preserve and unlock</button><button data-modal-close>Cancel</button></div><p data-quarantine-status>No provider request will be retried by this action. A known generation is checked once more before it can be quarantined.</p>`,(d,closeD)=>{
      const input=$('[data-quarantine-confirm]',d),apply=$('[data-quarantine-apply]',d),status=$('[data-quarantine-status]',d);
      input.oninput=()=>apply.disabled=input.value!==required;
      apply.onclick=async()=>{
        if(input.value!==required||state.inflight.screenwriter)return;
        apply.disabled=true;input.disabled=true;state.inflight.screenwriter=true;
        try{
          const result=await request('/screenwriter/quarantine',{method:'POST',body:JSON.stringify({projectId,confirmation:required,privacyMode:currentPrivacyMode()})});
          if(!sameSession(projectId,epoch))return;
          if(result.project)applyPayload(result.project);
          closeD(false);setScreenwriterRecovery(projectId,{status:'checking',message:'Checking for any other linked Trinity attempt.'});
          await reconcileScreenwriterProject(projectId,{epoch,waitForTerminal:false});
          if(sameSession(projectId,epoch))toast(result.message||'The unrecoverable Trinity turn was preserved without Dominion billing.');
        }catch(error){if(sameSession(projectId,epoch)){if(error?.code==='screenwriter_generation_now_recoverable'||Number(error?.status)===409){closeD(false);setScreenwriterRecovery(projectId,{status:'checking',message:'OpenRouter status changed during the final safety check. Loading the authoritative Trinity recovery action.'});render();await reconcileScreenwriterProject(projectId,{epoch,waitForTerminal:false});if(sameSession(projectId,epoch))toast(readable(error),'error');}else{status.textContent=readable(error);input.disabled=false;input.value='';input.focus();}}}
        finally{if(sameSession(projectId,epoch)){state.inflight.screenwriter=false;if(!activeDialog?.node?.isConnected)render();}}
      };
    },'Preserve unrecoverable Trinity turn');
  }
  async function historyDialog(){
    if(!state.project.id)return toast('Save the project before opening history.','error');if(projectSwitching||projectEditLocked())return toast('Saved history is paused until the current project operation finishes or reconciles.','error');const projectId=state.project.id,epoch=checkpointEpoch,loadToken=uid();setProjectSwitching(loadToken);
    try{
      await persistDirtyEdits('Saved edits before opening history');const data=await request(`/projects/${encodeURIComponent(projectId)}/history`);if(!sameSession(projectId,epoch))return;const items=data.checkpoints||[];
      dialog(`<header><span class="dv-eyebrow">PROJECT HISTORY</span><button data-modal-close aria-label="Close project history">×</button></header><h2>Saved checkpoints</h2><p>Restoring creates a new durable history event; it does not delete later checkpoints.</p><div class="dv-history-list">${items.length?items.map(item=>`<article><div><b>${esc(item.label||item.type||`Checkpoint ${item.seq}`)}</b><small>${esc(item.at||'')} · #${Number(item.seq)}</small></div><button data-restore="${Number(item.seq)}">Restore</button></article>`).join(''):'<p class="dv-empty-note">No saved checkpoints yet.</p>'}</div>`,(d,closeD)=>{d.addEventListener('click',event=>{const button=event.target.closest('[data-restore]');if(!button)return;if(projectSwitching||projectEditLocked())return toast('Restore is paused until the current durable project operation finishes or reconciles.','error');const seq=Number(button.dataset.restore);if(!Number.isInteger(seq)||!confirm(`Restore checkpoint #${seq}?`))return;runProjectSwitch(d,async()=>{if(projectEditLocked())throw new Error('Another durable project operation started before restore.');await persistDirtyEdits('Saved edits before history restore');if(!sameSession(projectId,epoch))return;const expectedProjectRevision=Number(state.projectRevision);if(!Number.isInteger(expectedProjectRevision))throw Object.assign(new Error('Refresh this project before restoring saved history.'),{code:'video_project_revision_required'});const payload=await request(`/projects/${encodeURIComponent(projectId)}/restore`,{method:'POST',body:JSON.stringify({seq,expectedProjectRevision})});if(!sameSession(projectId,epoch))return;applyPayload(payload.project?.project?payload.project:payload);save();closeD();render();toast(`Restored checkpoint #${seq}.`);}).catch(error=>{if(sameSession(projectId,epoch)){button.disabled=false;toast(readable(error),'error');}});});},'Project history');
    }catch(error){if(sameSession(projectId,epoch))toast(readable(error),'error');}
    finally{if(projectSwitching===loadToken)setProjectSwitching(null);}
  }
  function resumePersistedJobs(payload){const jobs=Array.isArray(payload?.jobs)?payload.jobs:[];for(const job of jobs){const status=String(job?.status||'').toLowerCase();if(!job?.id||['failed','cancelled','rejected'].includes(status)||(['ready','completed','succeeded'].includes(status)&&job.hasLocalOutput))continue;activeJobs.set(job.id,{projectId:state.project.id,singleGeneration:false,timer:null});state.job={id:job.id,projectId:state.project.id,singleGeneration:false,message:job.message||`Resuming ${jobs.length} saved generation job${jobs.length===1?'':'s'}…`};poll(job.id,state.project.id,false);}}
  function setProjectSwitching(token,d=activeDialog?.node){projectSwitching=token;if(d)$$('[data-new],[data-folder],[data-open],[data-rename],[data-delete]',d).forEach(button=>button.disabled=!!token);}
  async function runProjectSwitch(d,work){if(projectSwitching){toast('Another project action is already running.','error');return false;}const token=uid();setProjectSwitching(token,d);try{await work();return true;}finally{if(projectSwitching===token){setProjectSwitching(null,d);if(state.open&&!activeDialog?.node?.isConnected)render(false);}}}
  async function dashboard(){
    if(!fullEditor())return toast('Projects are managed from a desktop session.','error');
    dialog(`<header><span class="dv-eyebrow">PROJECT VAULT</span><button data-modal-close aria-label="Close projects">×</button></header><h2>Your video projects</h2><p>Projects persist until you delete them. Full editing is available on desktop.</p><div class="dv-project-actions"><button class="dv-primary" data-new>New project</button><button data-folder>Choose mirror folder</button></div><div class="dv-project-list"><p class="dv-loading">Loading projects…</p></div>`,async(d,closeD)=>{
      const list=$('.dv-project-list',d),draw=projects=>{list.innerHTML=projects.length?projects.map(p=>`<article><span>◇</span><div><b>${esc(p.name)}</b><small>${esc(p.updatedAt||'Saved locally')}</small></div><button data-open="${esc(p.id)}">Open</button><button data-rename="${esc(p.id)}">Rename</button><button data-delete="${esc(p.id)}">Delete</button></article>`).join(''):'<p class="dv-empty-note">No saved projects yet. Create your first one.</p>';};
      try{const data=await request('/projects');if(d.isConnected)draw(data.projects||[]);}catch(error){if(d.isConnected)list.innerHTML=`<p class="dv-empty-note">${esc(readable(error))}</p>`;}
      $('[data-new]',d).onclick=()=>runProjectSwitch(d,async()=>{if(!await prepareProjectSwitch())return;const epoch=checkpointEpoch,config=state.config,desktopSession=state.desktopSession,next={...defaults(),open:true,config,desktopSession},made=await request('/projects',{method:'POST',body:JSON.stringify({name:next.project.name})});if(!state.open||checkpointEpoch!==epoch)return;state=next;const payload=made.project?.project?made.project:made;applyPayload(payload);save();closeD();render();resumePersistedJobs(payload);toast('New video project created.');}).catch(error=>toast(readable(error),'error'));
      $('[data-folder]',d).onclick=()=>{if(projectSwitching||projectEditLocked())return toast('Folder choice is paused until the current durable project operation finishes or reconciles.','error');if(!window.showDirectoryPicker)return toast('Folder choice needs a desktop browser with File System Access support.');runProjectSwitch(d,async()=>{const epoch=checkpointEpoch,projectId=state.project.id,selected=await window.showDirectoryPicker({mode:'readwrite'});if(!state.open||checkpointEpoch!==epoch||state.project.id!==projectId||projectEditLocked())return;folderHandle=selected;state.project.folder=folderHandle.name;editRevision++;await checkpoint('Selected project mirror folder');toast(`Project checkpoints will also save in ${folderHandle.name}.`);}).catch(error=>{if(error?.name!=='AbortError')toast(readable(error),'error');});};
      d.onclick=e=>{const b=e.target.closest('[data-open],[data-rename],[data-delete]');if(!b||b.disabled)return;const id=b.dataset.open||b.dataset.rename||b.dataset.delete;
        if(b.dataset.open)runProjectSwitch(d,async()=>{if(!await prepareProjectSwitch())return;const epoch=checkpointEpoch,p=await request(`/projects/${encodeURIComponent(id)}`),payload=p.project?.project?p.project:p;if(!state.open||checkpointEpoch!==epoch)return;state.history=[];state.redo=[];state.job=null;applyPayload(payload);setScreenwriterRecovery(id,{status:'checking',message:'Checking this project for an unfinished Trinity turn.'});save();closeD();render();resumePersistedJobs(payload);void reconcileScreenwriterProject(id,{epoch,waitForTerminal:true});}).catch(error=>toast(readable(error),'error'));
        if(b.dataset.rename){const row=b.closest('article'),oldName=$('b',row)?.textContent||state.project.name,n=prompt('New project name',oldName);if(n?.trim())runProjectSwitch(d,async()=>{const epoch=checkpointEpoch,active=state.project.id===id;if(active){await persistDirtyEdits('Saved edits before project rename');if(!sameSession(id,epoch))return;}const renamed=await request(`/projects/${encodeURIComponent(id)}`,{method:'PATCH',body:JSON.stringify({name:n.trim()})});if(!state.open||checkpointEpoch!==epoch)return;if(active&&state.project.id===id)applyPayload(renamed.project?.project?renamed.project:renamed);toast('Project renamed.');dashboard();}).catch(error=>toast(readable(error),'error'));}
        if(b.dataset.delete&&confirm('Delete this project and its saved media? This cannot be undone.'))runProjectSwitch(d,async()=>{const activeId=state.project.id;if(activeId===id&&!await prepareProjectSwitch())return;if(activeId===id&&state.project.id!==id)throw new Error('The active project changed before deletion; nothing was deleted.');const epoch=checkpointEpoch;await request(`/projects/${encodeURIComponent(id)}`,{method:'DELETE',body:JSON.stringify({confirmDelete:true})});if(!state.open||checkpointEpoch!==epoch)return;if(activeId===id){const config=state.config,desktopSession=state.desktopSession;state={...defaults(),open:true,config,desktopSession};render(false);}toast('Project deleted.');dashboard();}).catch(error=>toast(readable(error),'error'));
      };
    },'Video projects');
  }
  function mobileNav(){dialog(`<header><span class="dv-eyebrow">VIDEO PANELS</span><button data-modal-close aria-label="Close panel switcher">×</button></header><h2>Switch workspace</h2><button data-modal-close data-mobile="writer">Screenwriter</button><button data-modal-close data-mobile="board">Storyboard</button><button data-modal-close data-mobile="timeline">Timeline</button><p class="dv-mobile-only">${state.desktopSession?'Connected to your desktop session. Changes save there.':'No desktop session detected. Single video generation only; generated videos download to this phone.'}</p>`,(d)=>$$('[data-mobile]',d).forEach(button=>button.onclick=()=>{const panelName=button.dataset.mobile;if(panelName==='writer'||panelName==='board')mutate(`Opened ${panelName} mobile panel`,()=>{state.panels.writer=panelName==='writer'?'expanded':'regular';state.panels.board=panelName==='board'?'expanded':'regular';});if(panelName==='timeline'){if(!state.focus)mutate('Entered editor focus',()=>state.focus=true);setTimeout(()=>$('.dv-timeline-panel',root)?.scrollIntoView({behavior:'smooth',block:'start'}),0);}}), 'Video workspace switcher');}
  async function resumeMobileJob(){if(!isMobileSingle()||activeJobs.size)return;try{const data=await request('/jobs/recover-mobile'),job=data.jobs?.[0];if(!job?.id||!job.projectId)return;state.job={id:job.id,projectId:job.projectId,singleGeneration:true,message:'Resuming your saved mobile generation…'};activeJobs.set(job.id,{projectId:job.projectId,singleGeneration:true,timer:null});render();poll(job.id,job.projectId,true);}catch(error){toast(`Mobile generation recovery needs attention. ${readable(error)}`,'error');}}
  /*
   * DESKTOP CAPABILITY SELF-HEAL (2026-08-05). Capability tokens live in an in-process Map on the
   * server, so every redeploy invalidates every open tab: the next Generate 409'd with "Refresh
   * Dominion Video on this desktop" until a hard refresh, and the old heartbeat swallowed every
   * error, so nothing ever noticed. Now a failed heartbeat (or any 409 capability refusal routed
   * here) re-fetches /config, adopts the fresh token, and says so once in the chat log.
   */
  let capabilityHealing=false;
  async function refreshDesktopCapability(reason){
    if(capabilityHealing||!state.open)return false;capabilityHealing=true;
    try{const c=await request(`/config?device=${device()}`);if(!state.open)return false;state.config={...state.config,...c};state.desktopSession=c.desktopSession!==false;state.messages.push({role:'status',content:reason||'This tab reconnected to the video service after a server restart. Nothing was lost.'});render(false);return true;}
    catch{return false;}
    finally{capabilityHealing=false;}
  }
  async function heartbeat(){try{const p=await request('/presence',{method:'POST',body:JSON.stringify({device:device()})});const nextSingle=device()==='mobile'&&p.desktopSession===false;if(state.config&&state.config.singleGenerationOnly!==nextSingle){state.config={...state.config,desktopSession:p.desktopSession!==false,fullEditor:!nextSingle,singleGenerationOnly:nextSingle};state.desktopSession=p.desktopSession!==false;root?.classList.toggle('dv-mobile-solo',nextSingle);render();}else state.desktopSession=p.desktopSession!==false;if(isMobileSingle()&&!activeJobs.size)await resumeMobileJob();}catch{await refreshDesktopCapability();}}
  function isolateBackground(){backgroundState=[...document.body.children].filter(node=>node!==root).map(node=>({node,inert:!!node.inert,aria:node.getAttribute('aria-hidden')}));for(const item of backgroundState){item.node.inert=true;item.node.setAttribute('aria-hidden','true');}}
  function restoreBackground(){for(const item of backgroundState){if(!item.node.isConnected)continue;item.node.inert=item.inert;if(item.aria==null)item.node.removeAttribute('aria-hidden');else item.node.setAttribute('aria-hidden',item.aria);}backgroundState=[];}
  async function open(){
    if(state.open)return;const epoch=++checkpointEpoch;studioReturnFocus=document.activeElement;state={...defaults(),open:true};preferenceKey=null;root=document.createElement('section');root.id='dv-root';root.tabIndex=-1;root.setAttribute('role','dialog');root.setAttribute('aria-modal','true');root.setAttribute('aria-labelledby','dv-studio-name');document.body.append(root);isolateBackground();document.body.classList.add('dv-open');render();root.focus();
    try{
      const c=await request(`/config?device=${device()}`);if(!state.open||checkpointEpoch!==epoch)return;state.config=c;state.desktopSession=c.desktopSession!==false;loadPreferences(c.storageKey);constrainModel(state.project.model);root.classList.toggle('dv-mobile-solo',c.singleGenerationOnly===true);let recoveryProjectId=null;
      if(!isMobileSingle()&&state.project.id){const projectId=state.project.id;try{const localUi={panels:clone(state.panels),focus:state.focus,zoom:state.zoom},loaded=await request(`/projects/${encodeURIComponent(projectId)}`);if(!sameSession(projectId,epoch))return;const payload=loaded.project?.project?loaded.project:loaded;applyPayload(payload);Object.assign(state,localUi);constrainModel(state.project.model);setScreenwriterRecovery(projectId,{status:'checking',message:'Checking this project for an unfinished Trinity turn.'});resumePersistedJobs(payload);recoveryProjectId=projectId;}catch(error){if(checkpointEpoch!==epoch||!state.open)return;state.project.id=null;state.screenplaySha256=null;state.screenwriterRecovery={pending:false,projectId:null,status:'idle',actionRequired:false,message:''};save();toast(`Saved project could not be refreshed. ${readable(error)}`,'error');}}
      render(false);if(recoveryProjectId)void reconcileScreenwriterProject(recoveryProjectId,{epoch,waitForTerminal:true});await heartbeat();if(!state.open||checkpointEpoch!==epoch)return;clearInterval(presenceTimer);presenceTimer=setInterval(heartbeat,20000);setTimeout(()=>$('[data-action="close"]',root)?.focus(),0);
    }catch(error){if(state.open&&checkpointEpoch===epoch){state.desktopSession=device()==='desktop';toast(readable(error),'error');}}
  }
  async function close(){if(!state.open||closing)return;if(conflictDraft){conflictDialog();return toast('Download the held local draft and load the newer server copy before closing Video Studio.','error');}if(projectSwitching||state.inflight.chat||state.inflight.generation||state.inflight.import||state.inflight.export)return toast('Wait for the active project operation to reach its durable checkpoint before closing Video Studio.','error');closing=true;captureDrafts();if(root){root.inert=true;root.setAttribute('aria-busy','true');}const hadScriptSave=!!scriptSaveTimer;clearTimeout(scriptSaveTimer);scriptSaveTimer=null;try{if(screenwriterSetup)await screenwriterSetup;if((hadScriptSave||editRevision>savedRevision)&&!state.inflight.screenwriter&&!state.screenwriterRecovery?.pending)await checkpoint('Saved edits before closing');await flushCheckpoints();}catch(error){closing=false;if(root){root.inert=false;root.removeAttribute('aria-busy');}toast(`Video Studio stayed open because its last change was not saved. ${readable(error)}`,'error');return;}checkpointEpoch++;creatingProject=null;screenwriterSetup=null;projectSwitching=null;state.open=false;state.playing=false;clearInterval(presenceTimer);presenceTimer=null;for(const entry of activeJobs.values())clearTimeout(entry.timer);activeJobs.clear();activeDialog?.close(false);activeDialog=null;save();document.body.classList.remove('dv-open');root?.remove();root=null;restoreBackground();studioReturnFocus?.focus?.();studioReturnFocus=null;closing=false;}
  function init(){ if(!document.getElementById('dominion-video-style')){const l=document.createElement('link');l.id='dominion-video-style';l.rel='stylesheet';l.href='/dominion-video.css?v=5';document.head.append(l);} if(!document.getElementById('dv-launch')){const b=document.createElement('button');b.id='dv-launch';b.type='button';b.innerHTML='<span>◇</span> Video Studio';b.title='Open Dominion Video Studio';b.onclick=open; const anchor=$('.command-actions')||document.body;anchor.append(b);} window.addEventListener('keydown',e=>{if(!state.open)return;if(activeDialog)return;const editable=e.target instanceof HTMLElement&&e.target.closest('input,textarea,select,[contenteditable="true"]');if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='z'&&!editable){e.preventDefault();e.shiftKey?redo():undo();return;}if(e.key==='Escape'){e.preventDefault();if(state.focus)mutate('Exited editor focus',()=>state.focus=false);else close();return;}if(e.key==='Tab')trap(e,root);}); }
  window.addEventListener('beforeunload',event=>{if(conflictDraft){event.preventDefault();event.returnValue='';}});
  window.DominionVideo={init,open,close,get state(){return state;},getState:()=>clone(projectPayload())};
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();
